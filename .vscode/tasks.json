{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "ralphdex: compile extension",
            "type": "shell",
            "command": "npm run compile",
            "options": {
                "cwd": "${workspaceFolder}/ralph-codex-vscode-starter"
            },
            "problemMatcher": "$tsc",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
