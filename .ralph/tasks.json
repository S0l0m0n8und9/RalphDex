{
  "version": 2,
  "tasks": [
    {
      "id": "T1",
      "title": "Write or refine the project objective in the PRD file",
      "status": "done",
      "notes": "The prompt generator reads the PRD file directly."
    },
    {
      "id": "T2",
      "title": "Replace this seed task list with repo-specific work",
      "status": "done",
      "notes": "Keep statuses current so fresh Codex runs can resume deterministically."
    },
    {
      "id": "T3",
      "title": "Detect nested project roots so Ralph can inspect the starter extension from the repo root",
      "status": "done",
      "notes": "Workspace scanning now selects the nested `ralph-codex-vscode-starter` project root when the repo root has no shallow markers, and the runtime build was refreshed so the extension uses the updated scan path.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T4",
      "title": "Add regression coverage for nested workspace scanning and prompt repo-context rendering",
      "status": "done",
      "dependsOn": [
        "T3"
      ],
      "notes": "Locked in nested root-selection evidence plus prompt repo-context rendering for inspected-root versus workspace-root cases.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T5",
      "title": "Cover clipboard and IDE handoff strategies with automated tests and explicit docs",
      "status": "done",
      "notes": "Added command-shell smoke coverage for prompt clipboard auto-copy plus Open Codex IDE clipboard-only, IDE-command, missing-command fallback, and cliExec-warning handoff paths, and documented the operator-facing handoff rules and remaining live-host gaps.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T6",
      "title": "Prove the packaging path and document the manual `.vsix` install workflow",
      "status": "done",
      "notes": "Documented the operator-facing `npm run package` plus manual `.vsix` install workflow, hardened the package script for this repo by using `vsce package --no-dependencies`, added repository metadata plus README links that `vsce` can rewrite, and validated a fresh `ralph-codex-workbench-0.1.0.vsix` build. Packaging still warns about a missing LICENSE file and the lack of `.vscodeignore` or a `files` allowlist.",
      "validation": "cd ralph-codex-vscode-starter && npm run package"
    },
    {
      "id": "T7",
      "title": "Exercise a real CLI iteration against a temp workspace and tighten any artifact or verifier gaps it exposes",
      "status": "done",
      "dependsOn": [
        "T3",
        "T4"
      ],
      "notes": "Added an optional `npm run test:real-cli-smoke` temp-workspace runner that drives the actual `codex exec` path, documented its operator use and environment limits, and surfaced summarized execution failure messages into the structured iteration/latest-result/status artifacts. A real smoke run in this sandbox exercised the path and preserved a temp workspace with the expected backend-connect failure evidence.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T8",
      "title": "Align nested inspection-root and execution-root semantics across prompting, execution, and verification",
      "status": "done",
      "dependsOn": [
        "T3",
        "T4",
        "T7"
      ],
      "notes": "Ralph now persists explicit workspace/inspection/execution/verification root policy in prompt evidence, execution plans, CLI invocation artifacts, iteration results, and provenance bundles. `codex exec` plus validation/git verifiers run from the selected inspection root while `.ralph` stays anchored at the workspace root, and `ralphCodex.inspectionRootOverride` now provides a deterministic manual override for ambiguous umbrella workspaces with evidence, status, docs, and regression coverage.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T8.1",
      "title": "Persist chosen inspection root in prompt evidence, execution plans, provenance bundles, and status surfaces",
      "status": "done",
      "parentId": "T8",
      "dependsOn": [
        "T3",
        "T4"
      ],
      "notes": "Added explicit root-policy persistence covering workspace, inspection, execution, and verification roots in prompt evidence, execution plans, CLI invocation artifacts, iteration results, provenance bundles, and status summaries.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T8.2",
      "title": "Define deterministic execution-root and verifier-root policy for nested repos",
      "status": "done",
      "parentId": "T8",
      "dependsOn": [
        "T8.1"
      ],
      "notes": "Locked in a simple policy: execution root and verifier root follow the selected inspection root; workspace root continues to own `.ralph` control-plane state and artifacts.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T8.3",
      "title": "Implement CLI and verifier root-alignment using the chosen root policy",
      "status": "done",
      "parentId": "T8",
      "dependsOn": [
        "T8.2"
      ],
      "notes": "CLI execution now passes the selected execution root into `codex exec --cd`, and validation/git verifiers run from the selected verification root instead of depending on manual `cd ... && ...` command composition.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T8.4",
      "title": "Add an explicit inspection-root override for ambiguous umbrella or multi-repo workspaces",
      "status": "done",
      "parentId": "T8",
      "dependsOn": [
        "T8.2"
      ],
      "notes": "Added the `ralphCodex.inspectionRootOverride` setting, validated it against the workspace boundary, let it override shallow root scoring when valid, and surfaced applied/invalid override state in workspace-scan evidence and status output.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T8.5",
      "title": "Add regression coverage for nested execution-root, verifier-root, and override behavior",
      "status": "done",
      "parentId": "T8",
      "dependsOn": [
        "T8.3",
        "T8.4"
      ],
      "notes": "Regression coverage now includes applied and invalid inspection-root override scanning, status rendering for override state, nested execution-root and verifier-root persistence, and an end-to-end CLI iteration proving override-driven execution/verifier cwd selection.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T8.6",
      "title": "Document workspace-root, inspection-root, execution-root, and verifier-root behavior",
      "status": "done",
      "parentId": "T8",
      "dependsOn": [
        "T8.3",
        "T8.4"
      ],
      "notes": "Updated README plus architecture, provenance, and verifier docs so nested-root execution semantics, the workspace-root `.ralph` boundary, and the `inspectionRootOverride` escape hatch are explicit.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T9",
      "title": "Defer broad multi-agent orchestration until nested root semantics are deterministic and evidence-backed",
      "status": "done",
      "dependsOn": [
        "T8"
      ],
      "notes": "Locked the current product boundary to a sequential single-agent loop, documented that broader multi-agent orchestration remains deferred until nested workspace/inspection/execution/verification root policy is deterministic, test-backed, and evidence-backed, and added docs-validation coverage so that guardrail stays explicit."
    },
    {
      "id": "T10",
      "title": "Add an explicit inspection-root override for ambiguous umbrella or multi-repo workspaces",
      "status": "done",
      "parentId": "T9",
      "dependsOn": [
        "T8.2"
      ],
      "notes": "Satisfied by the `ralphCodex.inspectionRootOverride` implementation completed under T8.4; retained here only as the previously-recorded duplicate follow-on item.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T11",
      "title": "Add bounded retention for iteration, prompt, and transcript artifacts so repeated Ralph loops do not grow unbounded",
      "status": "done",
      "notes": "Surfaced generated artifact retention in Show Status, added explicit enabled/disabled coverage, and aligned testing docs with the existing prompt/run/iteration cleanup behavior.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T12",
      "title": "Protect latest-linked artifacts and state-referenced run files when non-provenance retention cleanup executes",
      "status": "done",
      "dependsOn": [
        "T11"
      ],
      "notes": "Added explicit regression and doc coverage proving latest-preflight-report.json protects only the referenced iteration directory during generated-artifact cleanup; npm test passed.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T12.1",
      "title": "Identify the protected latest pointers and state references that non-provenance cleanup must honor",
      "status": "done",
      "parentId": "T12",
      "dependsOn": [
        "T11"
      ],
      "notes": "Enumerate exactly which latest-pointer JSON artifacts and `.ralph/state.json` references count as protected roots for generated prompts, run transcripts/last-message pairs, and iteration directories.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T12.2",
      "title": "Define deterministic retention precedence rules when generated-artifact cleanup and protected references conflict",
      "status": "done",
      "parentId": "T12",
      "dependsOn": [
        "T12.1"
      ],
      "notes": "Made generated-artifact retention precedence explicit in the high-level README and architecture docs: Ralph keeps the newest parsed-iteration window first, then adds older protected references without evicting newer retained entries.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T12.3",
      "title": "Implement protected-artifact retention for latest-linked prompts, summaries, transcripts, and iteration directories",
      "status": "done",
      "parentId": "T12",
      "dependsOn": [
        "T12.2"
      ],
      "notes": "Update non-provenance cleanup so it preserves the newest prompt, transcript/last-message pair, preflight/summary surface, or iteration directory still referenced by stable latest pointers or workspace state.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T12.4",
      "title": "Add targeted regression coverage for protected latest-linked and state-referenced generated artifacts",
      "status": "done",
      "parentId": "T12",
      "dependsOn": [
        "T12.3"
      ],
      "notes": "Added focused artifact-retention regressions for latest-linked prompt/summary/preflight references and state-referenced transcript, last-message, and iteration-directory roots; updated testing docs accordingly.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T13",
      "title": "Detect and repair stale latest-artifact pointers when operators manually delete generated Ralph files",
      "status": "done",
      "dependsOn": [
        "T12"
      ],
      "notes": "Added deterministic repair for deleted latest summary/provenance summary surfaces from surviving latest JSON metadata, wired status/open-latest flows through that repair path, and covered manual-deletion regressions.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T14",
      "title": "Expand Show Status with recent iteration and run history plus artifact-retention summaries",
      "status": "done",
      "dependsOn": [
        "T13"
      ],
      "notes": "Expanded Show Status with recent iteration/run history, live generated-artifact and provenance-bundle retention summaries, and latest-artifact repair/stale reporting; added rendering tests and aligned workflow/testing docs.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T15",
      "title": "Add direct commands to open the latest prompt evidence and latest CLI transcript or last-message artifacts",
      "status": "done",
      "dependsOn": [
        "T13"
      ],
      "notes": "Added direct commands to open the latest prompt-evidence artifact and the latest CLI transcript with last-message fallback, plus registration/docs/smoke coverage.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T16",
      "title": "Surface stale-artifact and retention-readiness diagnostics in preflight before a loop starts",
      "status": "done",
      "dependsOn": [
        "T13"
      ],
      "notes": "Added preflight artifact-readiness diagnostics for stale latest surfaces, missing latest-pointer targets, unsafe retention-root overlap, and disabled retention settings; wired them into iteration-start and status preflight paths; added regression coverage and doc updates.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T17",
      "title": "Add a scoped cleanup workflow that prunes generated runtime artifacts without fully resetting durable loop state",
      "status": "done",
      "dependsOn": [
        "T16"
      ],
      "notes": "Added a scoped Cleanup Runtime Artifacts command that preserves durable Ralph state and latest evidence while pruning older generated prompts, run artifacts, iteration directories, provenance bundles, and logs; added regression coverage and updated docs.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T18",
      "title": "Cover retention, stale-pointer recovery, latest-artifact commands, and scoped cleanup with regression tests",
      "status": "done",
      "dependsOn": [
        "T14",
        "T15",
        "T17"
      ],
      "notes": "Aligned AGENTS.md with the new latest-artifact and cleanup command surface added for T18; npm run validate passes cleanly on the current worktree.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T19",
      "title": "Document artifact lifecycle, retention, recovery, and cleanup workflows for long-running Ralph loops",
      "status": "done",
      "dependsOn": [
        "T14",
        "T15",
        "T17"
      ],
      "notes": "README plus focused docs should explain what Ralph retains, what cleanup may delete, how stale artifacts are repaired, and which inspection commands to use during a long loop.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T20",
      "title": "Remove the remaining `.vsix` packaging warnings and revalidate the release-build workflow after artifact-lifecycle changes",
      "status": "done",
      "dependsOn": [
        "T19"
      ],
      "notes": "Removed the remaining vsce packaging warnings by adding a bundled MIT LICENSE file and a package.json files allowlist, updated release-build docs to describe the curated VSIX payload, and revalidated with npm run package plus npm run check:docs.",
      "validation": "cd ralph-codex-vscode-starter && npm run package"
    },
    {
      "id": "T21",
      "title": "Add a bounded task-remediation pass for repeatedly failing or no-progress Ralph tasks",
      "status": "done",
      "dependsOn": [
        "T18",
        "T19"
      ],
      "notes": "Added a bounded, deterministic task-remediation recommendation for repeated no-progress and repeated identical failure outcomes, surfaced it in iteration/state artifacts and status/prompt context, and covered it with tests and docs.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T21.1",
      "title": "Define deterministic triggers and outcomes for task remediation",
      "status": "done",
      "parentId": "T21",
      "dependsOn": [
        "T18"
      ],
      "notes": "Specify when remediation activates, such as repeated no_progress, repeated identical failure signatures, or repeated blocked starts on the same task. Define allowed outcomes: decompose_task, reframe_task, mark_blocked, request_human_review, or no_action.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T21.2",
      "title": "Add remediation evidence artifacts and status reporting",
      "status": "done",
      "parentId": "T21",
      "dependsOn": [
        "T21.1"
      ],
      "notes": "Persist a remediation proposal artifact with the triggering history, rationale, proposed action, and any suggested child tasks. Surface the latest remediation status in Show Status and latest-summary flows.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T21.3",
      "title": "Implement bounded task-decomposition proposals for overly broad failed tasks",
      "status": "done",
      "parentId": "T21",
      "dependsOn": [
        "T21.1",
        "T21.2"
      ],
      "notes": "Implemented bounded structured child-task proposals for repeated no-progress remediation artifacts, surfaced proposal details in status output, updated docs, and validated with npm run validate.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T21.4",
      "title": "Add a human-reviewed apply path for accepted decomposition proposals",
      "status": "done",
      "parentId": "T21",
      "dependsOn": [
        "T21.3"
      ],
      "notes": "Added an explicit operator-approved command that applies the latest accepted decomposition proposal into .ralph/tasks.json while keeping remediation defaulted to propose-only.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T21.5",
      "title": "Add regression coverage for remediation triggers, proposal artifacts, and approved task decomposition",
      "status": "done",
      "parentId": "T21",
      "dependsOn": [
        "T21.4"
      ],
      "notes": "Cover repeated no_progress, repeated failure signatures, blocked-start cases, proposal generation, non-decomposition cases, and approved application into tasks.json.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T21.6",
      "title": "Document task remediation, decomposition limits, and human-approval boundaries",
      "status": "done",
      "parentId": "T21",
      "dependsOn": [
        "T21.4",
        "T21.5",
        "T21.6.1",
        "T21.6.2"
      ],
      "notes": "Documented remediation triggers, bounded outcomes, proposal artifacts, operator approval flow, and the hard limits that keep remediation deterministic and non-chaotic across README plus verifier and workflow docs.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T21.6.1",
      "title": "Document task remediation, decomposition limits,",
      "status": "done",
      "parentId": "T21.6",
      "dependsOn": [
        "T21.4",
        "T21.5",
        "T21.6.1.1",
        "T21.6.1.2",
        "T21.6.1.3"
      ],
      "notes": "Closed the bounded-decomposition wording gap by documenting the deterministic reproduce-fix-rerun sequence directly in the remediation docs and validating it with the docs checker.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T21.6.2",
      "title": "Human-approval boundaries",
      "status": "done",
      "parentId": "T21.6",
      "dependsOn": [
        "T21.4",
        "T21.5",
        "T21.6.1"
      ],
      "notes": "Documented the operator-reviewed apply path and explicit no-auto-rewrite boundaries so remediation remains propose-only until a human accepts the decomposition.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T22",
      "title": "Add token-budgeted prompt and context generation for Ralph CLI and IDE handoff flows",
      "status": "done",
      "dependsOn": [
        "T19",
        "T22.1",
        "T22.2",
        "T22.3",
        "T22.4",
        "T22.5",
        "T22.6",
        "T22.7"
      ],
      "notes": "Implemented prompt-budget policy, bounded objective/progress/prior-evidence summarization, task-aware repo-context trimming, prompt-budget evidence artifacts, default-medium CLI reasoning effort, status visibility, regression coverage, and operator docs. Validation passed with npm run validate.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T22.1",
      "title": "Define prompt-budget policy and minimum required context for each prompt kind",
      "status": "done",
      "parentId": "T22",
      "dependsOn": [
        "T22.1.1",
        "T22.1.2"
      ],
      "notes": "Defined deterministic prompt-budget policies for each prompt kind and target, including required sections, optional sections, omission order, and minimum-context bias persisted in prompt evidence.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T22.2",
      "title": "Implement compact objective, progress, and prior-evidence summarization for prompt rendering",
      "status": "done",
      "parentId": "T22",
      "dependsOn": [
        "T22.1"
      ],
      "notes": "Replaced large objective/progress snapshots with bounded summaries and filtered prior-iteration context toward the selected task and recent failure pattern.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T22.3",
      "title": "Make repo-context sections task-aware and omit irrelevant fields by default",
      "status": "done",
      "parentId": "T22",
      "dependsOn": [
        "T22.1"
      ],
      "notes": "Made repo-context selection task-aware so docs-focused prompts skip irrelevant code/package inventory while code-focused work still keeps source and validation context.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T22.4",
      "title": "Add prompt-size accounting and prompt-budget evidence artifacts",
      "status": "done",
      "parentId": "T22",
      "dependsOn": [
        "T22.2",
        "T22.3"
      ],
      "notes": "Persisted prompt byte counts, estimated token range, budget outcome, section selection, omission order, and budget mode in prompt-evidence and surfaced the same inspection path in Show Status.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T22.5",
      "title": "Default CLI reasoning effort to medium and make high an explicit escalation path",
      "status": "done",
      "parentId": "T22",
      "dependsOn": [
        "T22.1"
      ],
      "notes": "Added default-medium CLI reasoning effort, threaded it through codex exec as model_reasoning_effort, persisted the chosen value in invocation/integrity artifacts, and documented high as an explicit escalation path.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T22.6",
      "title": "Add regression coverage for prompt budgeting, section omission, and reasoning-effort defaults",
      "status": "done",
      "parentId": "T22",
      "dependsOn": [
        "T22.4",
        "T22.5"
      ],
      "notes": "Added regression coverage for prompt-budget policy selection, oversized-prompt trimming, task-aware section inclusion, prompt-budget evidence persistence, status rendering, and default-versus-escalated reasoning behavior.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T22.7",
      "title": "Document prompt-budget policy, context selection rules, and quota-saving operator guidance",
      "status": "done",
      "parentId": "T22",
      "dependsOn": [
        "T22.4",
        "T22.5",
        "T22.6"
      ],
      "notes": "Documented prompt-budget policy, omission order, reasoning-effort defaults, and quota-sensitive operator guidance in workflows and provenance docs.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T21.6.1.1",
      "title": "Reproduce the blocker with a deterministic verification target",
      "status": "done",
      "parentId": "T21.6.1",
      "dependsOn": [
        "T21.4",
        "T21.5"
      ],
      "notes": "Documented that bounded decomposition should start by reproducing the blocker against the inherited validation command, and added docs-validator coverage so the deterministic verification-target rule stays enforced.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T21.6.1.2",
      "title": "Implement the smallest bounded fix for that narrowed blocker",
      "status": "done",
      "parentId": "T21.6.1",
      "dependsOn": [
        "T21.4",
        "T21.5",
        "T21.6.1.1"
      ],
      "notes": "Documented that the next narrowed child should implement the smallest bounded fix for the reproduced blocker instead of bundling extra cleanup or another planning pass.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T21.6.1.3",
      "title": "Rerun verification and capture the bounded evidence",
      "status": "done",
      "parentId": "T21.6.1",
      "dependsOn": [
        "T21.4",
        "T21.5",
        "T21.6.1.2"
      ],
      "notes": "Revalidated the remediation-doc branch with npm run check:docs after reconciling the ledger so the reproduce-fix-rerun sequence is evidenced by the docs checker.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T22.1.1",
      "title": "Define prompt-budget policy",
      "status": "done",
      "parentId": "T22.1",
      "notes": "Defined per-kind per-target prompt-budget policies with explicit token targets, context bias, required sections, and optional omission order.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T22.1.2",
      "title": "Minimum required context for each prompt kind",
      "status": "done",
      "parentId": "T22.1",
      "dependsOn": [
        "T22.1.1"
      ],
      "notes": "Locked the minimum required prompt sections so quota trimming never drops strategy, preflight, objective, task, operating rules, execution contract, or final response contract.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T23",
      "title": "Harden task-ledger integrity so tracker drift cannot silently exhaust or misstate the backlog",
      "status": "done",
      "dependsOn": [
        "T23.1",
        "T23.2",
        "T23.3",
        "T23.4"
      ],
      "notes": "Auto-completed satisfied aggregate parent tasks during completion-report reconciliation so child-complete ledger drift no longer leaves stale backlog items behind."
    },
    {
      "id": "T23.1",
      "title": "Detect done parent tasks that still have unfinished descendants",
      "status": "done",
      "parentId": "T23",
      "dependsOn": [
        "T22",
        "T23.2"
      ],
      "notes": "Re-inspected durable repo state and confirmed T23.1 is already implemented: done parents with unfinished descendants are reported as task-ledger drift in task-file inspection, surfaced through preflight/status consumers, and covered by regression tests. No code edits were necessary.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T23.2",
      "title": "Surface task-ledger drift in preflight, status, and backlog-replenishment context",
      "status": "done",
      "parentId": "T23",
      "dependsOn": [
        "T22"
      ],
      "notes": "Surfaced task-ledger drift explicitly in preflight summaries, status output, and replenish-backlog prompt selection/strategy so inconsistent ledgers no longer read as clean backlog exhaustion. Added regression coverage for the new wording and prompt decision path.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T23.3",
      "title": "Add regression coverage for ledger-drift diagnostics and exhausted-backlog handling",
      "status": "done",
      "parentId": "T23",
      "dependsOn": [
        "T23.1",
        "T23.2"
      ],
      "notes": "Added regression coverage for done-parent ledger-drift diagnostics, exhausted-backlog loop stopping, drift-specific preflight blocking, and status rendering.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T23.4",
      "title": "Document task-ledger integrity rules and the repair path for drifted backlogs",
      "status": "done",
      "parentId": "T23",
      "dependsOn": [
        "T23.2",
        "T23.3"
      ],
      "notes": "Documented task-ledger integrity rules, explicit drift warnings, and the operator repair-vs-replenish decision path in the invariant and workflow docs. Validation passed with npm run check:docs.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T24",
      "title": "Multi-agent readiness: task ownership, concurrent reconciliation, and remediation isolation",
      "status": "todo",
      "dependsOn": [
        "T24.1",
        "T24.2",
        "T24.3",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "Three concrete gaps block safe multi-agent operation: tasks.json has no locking so concurrent agents corrupt on last-write-wins; iterationEngine reconciles completion reports sequentially with no concurrency guard; buildTaskRemediation counts trailing classifications from a shared history so cross-agent signals produce false positives. All three must be addressed before the multi-agent deferral in docs/boundaries.md can be lifted."
    },
    {
      "id": "T24.1",
      "title": "Add atomic task-claim mechanism so agents cannot concurrently select the same task",
      "status": "todo",
      "parentId": "T24",
      "dependsOn": [
        "T24.1.1",
        "T24.1.2",
        "T24.1.3",
        "T24.1.4",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "tasks.json is a flat file with a single-writer assumption. Two agents selecting the same task produce last-write-wins corruption that T23 drift detection catches only after damage is done. The fix is an atomic claim step: an agent writes a claim record before execution begins, the control plane validates the claim is still current before reconciling the completion report, and stale or contested claims are surfaced in preflight."
    },
    {
      "id": "T24.1.1",
      "title": "Define task-claim schema and add it to types.ts",
      "status": "done",
      "parentId": "T24.1",
      "notes": "Added RalphTaskClaimStatus, RalphTaskClaim, and RalphTaskClaimFile to src/ralph/types.ts for a flat, inspectable task-claim schema consistent with tasks.json.",
      "validation": "cd ralph-codex-vscode-starter && npm run compile"
    },
    {
      "id": "T24.1.2",
      "title": "Implement claim acquisition and release in taskFile.ts with file-level locking",
      "status": "todo",
      "parentId": "T24.1",
      "dependsOn": [
        "T24.1.1",
        "T24.1.2.1",
        "T24.1.2.2",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "Add acquireClaim(taskId, agentId, provenanceId) and releaseClaim(taskId, agentId) to src/ralph/taskFile.ts. Use a write-then-verify pattern: write the claim record, re-read the file, confirm the claim is still the canonical holder. If another claim exists for the same taskId, return a contested result without writing. Release must be idempotent. Stale claims older than a configurable TTL should be detectable but not auto-released — that is an operator decision.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T24.1.3",
      "title": "Gate task selection and completion-report reconciliation on current claim ownership",
      "status": "todo",
      "parentId": "T24.1",
      "dependsOn": [
        "T24.1.2",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "In src/ralph/iterationEngine.ts, call acquireClaim immediately after task selection and before prompt generation. In the completion-report reconciliation step, verify the claim is still active and owned by the current provenanceId before writing to tasks.json. If the claim has been released or superseded, surface a new stop reason: claim_contested, and persist the iteration result without mutating task state.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T24.1.4",
      "title": "Surface stale and contested claims in preflight and Show Status",
      "status": "todo",
      "parentId": "T24.1",
      "dependsOn": [
        "T24.1.3",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "Add a claimGraph category to preflight diagnostics in src/ralph/preflight.ts. Detect: tasks claimed by an agent with no corresponding active iteration (stale), tasks claimed by multiple agents (contested), and tasks whose claim provenanceId does not match the current iteration provenanceId. Surface claim state in src/ralph/statusReport.ts alongside the existing task-selection summary. Add regression coverage and update docs/invariants.md with the claim ownership rules.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T24.2",
      "title": "Serialise concurrent completion-report reconciliation writes to tasks.json",
      "status": "todo",
      "parentId": "T24",
      "dependsOn": [
        "T24.2.1",
        "T24.2.2",
        "T24.2.3",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "iterationEngine.ts reconciles one completion report at a time against tasks.json with no concurrency guard. Concurrent writes from multiple agents hitting the reconciliation path simultaneously will produce last-write-wins corruption even if claims are correctly acquired. The reconciliation path needs a write-serialisation layer that is deterministic, file-backed, and does not require an external lock server."
    },
    {
      "id": "T24.2.1",
      "title": "Add a file-backed write-serialisation helper for tasks.json mutations",
      "status": "todo",
      "parentId": "T24.2",
      "dependsOn": [
        "T24.1.2",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "Add a withTaskFileLock(fn) helper in src/ralph/taskFile.ts that acquires an exclusive advisory lock on tasks.json before calling fn, then releases it. Use Node's fs.open with O_EXCL on a sibling .ralph/tasks.lock file as the advisory mechanism. Lock acquisition must be non-blocking with a configurable retry count and backoff so a waiting agent does not spin. If the lock cannot be acquired within the retry window, return a lock_timeout result that the caller can surface as a preflight warning rather than silently corrupting state.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T24.2.2",
      "title": "Wrap all tasks.json mutation paths in the write-serialisation helper",
      "status": "todo",
      "parentId": "T24.2",
      "dependsOn": [
        "T24.2.1",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "Identify every call site in iterationEngine.ts, taskFile.ts, and registerCommands.ts that writes to tasks.json: completion-report reconciliation, Apply Latest Task Decomposition Proposal, and any direct task-status updates. Wrap each in withTaskFileLock. The lock scope should be as narrow as possible: acquire immediately before the read-modify-write cycle, release immediately after. Do not hold the lock across codex exec execution.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T24.2.3",
      "title": "Add regression coverage and document the serialisation contract in docs/invariants.md",
      "status": "todo",
      "parentId": "T24.2",
      "dependsOn": [
        "T24.2.2",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "Add tests in test/taskFile.test.ts covering: concurrent write attempts where the second waits and succeeds after lock release, lock_timeout behaviour when the lock is held beyond the retry window, and lock cleanup on process exit. Document the write-serialisation contract in docs/invariants.md under a new Task Graph Write Serialisation section: which paths hold the lock, maximum hold duration, and what happens on timeout.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T24.3",
      "title": "Isolate remediation history per agent so cross-agent classifications do not trigger false repeated-stop signals",
      "status": "todo",
      "parentId": "T24",
      "dependsOn": [
        "T24.3.1",
        "T24.3.2",
        "T24.3.3",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "buildTaskRemediation() in loopLogic.ts counts trailing same-task classifications from a shared iterationHistory. With multiple agents, a repeated_no_progress signal on a task could come from two different agents alternating — the trigger logic misreads interleaved history as one agent spinning. The remediation window must be scoped to a single agent's contiguous run on a given task, not the global history."
    },
    {
      "id": "T24.3.1",
      "title": "Add agentId to RalphIterationResult and RalphRunRecord in types.ts",
      "status": "done",
      "parentId": "T24.3",
      "dependsOn": [
        "T24.1.1"
      ],
      "notes": "Added optional agentId to RalphIterationResult and RalphRunRecord, defaulted legacy/missing records to the 'default' sentinel in state normalization, and persisted the sentinel on newly created single-agent iteration/run records. Compile passed.",
      "validation": "cd ralph-codex-vscode-starter && npm run compile"
    },
    {
      "id": "T24.3.2",
      "title": "Scope countTrailingSameTaskClassifications to a single agentId in loopLogic.ts",
      "status": "todo",
      "parentId": "T24.3",
      "dependsOn": [
        "T24.3.1",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "Modify countTrailingSameTaskClassifications in src/ralph/loopLogic.ts to include agentId in the trailing-match predicate alongside selectedTaskId and classification. The trailing window breaks as soon as a history entry for the same task has a different agentId, preventing interleaved multi-agent history from accumulating into a false repeated-stop trigger. Single-agent runs using the default sentinel are unaffected.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T24.3.3",
      "title": "Add regression coverage for multi-agent remediation isolation and update docs",
      "status": "todo",
      "parentId": "T24.3",
      "dependsOn": [
        "T24.3.2",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "Add tests in test/loopLogic.test.ts covering: interleaved history from two agents on the same task does not trigger repeated_no_progress for either agent, a single agent accumulating the threshold count on the same task still triggers correctly, and the default sentinel preserves existing single-agent behaviour. Update docs/verifier.md to note that repeated-stop detection is scoped per agent and that multi-agent deployments should assign distinct agentId values.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T24.1.2.1",
      "title": "Claim acquisition",
      "status": "todo",
      "parentId": "T24.1.2",
      "dependsOn": [
        "T24.1.1",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "Narrow T24.1.2 to a deterministic first step before retrying the parent task.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T24.1.2.2",
      "title": "Release in taskFile.ts with file-level locking",
      "status": "todo",
      "parentId": "T24.1.2",
      "dependsOn": [
        "T24.1.1",
        "T24.1.2.1",
        "T25",
        "T26",
        "T27"
      ],
      "notes": "Keep the proposal one level deep by sequencing the next bounded step after T24.1.2.1.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T25",
      "title": "Remove operator workspace state from the committed repository",
      "status": "todo",
      "dependsOn": [
        "T25.1",
        "T25.2",
        "T25.3"
      ],
      "notes": "EXECUTE BEFORE T24. state.json (6265 lines of machine-specific runtime paths), done-task-audit-current-worktree.md, and logs/extension.log are committed to the repo, meaning every clone starts with broken path references pointing at /home/admin/Documents/repos/Ralph and a misleading preloaded iteration history. The .ralph/.gitignore already excludes /artifacts, /logs, /prompts, and /runs but misses state.json and ad-hoc audit files. This is the highest-priority fix because it unblocks contributors and corrects the credibility contradiction of a durable-state tool shipping a broken instance of its own durable state."
    },
    {
      "id": "T25.1",
      "title": "Extend .ralph/.gitignore to exclude runtime state and audit files, then remove tracked files",
      "status": "blocked",
      "parentId": "T25",
      "notes": "Updated .ralph/.gitignore to exclude /state.json and /done-task-audit*.md. Could not remove tracked runtime files from the Git index because the sandbox user is denied write access to .git, so .ralph/state.json, .ralph/done-task-audit-current-worktree.md, and .ralph/logs/extension.log remain tracked.",
      "validation": "git ls-files .ralph/ | grep -v 'prd.md\\|tasks.json\\|progress.md\\|\\.gitignore' | wc -l | grep -q '^0$'",
      "blocker": "Sandbox permissions deny writes to the repository .git directory, preventing git rm --cached from updating the index."
    },
    {
      "id": "T25.2",
      "title": "Add Ralph Codex: Initialize Workspace command that creates a clean .ralph scaffold for new clones",
      "status": "todo",
      "parentId": "T25",
      "dependsOn": [
        "T25.1"
      ],
      "notes": "Add a Ralph Codex: Initialize Workspace command in src/commands/registerCommands.ts. The command should: (1) check whether .ralph/prd.md exists and abort with a warning if it does to prevent overwriting active state; (2) create .ralph/ with an empty prd.md scaffold containing a placeholder objective comment; (3) create a minimal tasks.json with version 2 and an empty tasks array; (4) create an empty progress.md; (5) write the correct .ralph/.gitignore content if it does not already exist. Document the command in README.md under Getting Started and in docs/workflows.md under a new Initialize A Fresh Workspace section. Add it to package.json contributes.commands.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T25.3",
      "title": "Document the project-source versus operator-workspace-state distinction in boundaries and README",
      "status": "todo",
      "parentId": "T25",
      "dependsOn": [
        "T25.2"
      ],
      "notes": "Add a Repository Layout And Workspace State section to docs/boundaries.md that explicitly states: .ralph/prd.md, .ralph/tasks.json, and .ralph/progress.md are project artifacts committed alongside source when the repo itself is the Ralph workspace (as in this repo); .ralph/state.json, .ralph/logs/, .ralph/prompts/, .ralph/runs/, and .ralph/artifacts/ are operator-local runtime state and must not be committed. Add a brief note to the root README.md under Where Ralph Stores State clarifying which files are safe to commit. Add the required fragment 'operator-local runtime state' to the DOC_RULES entry for docs/boundaries.md in src/validation/docsValidator.ts.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T26",
      "title": "Replace the permanent multi-agent deferral with an explicit acceptance-criteria milestone",
      "status": "todo",
      "dependsOn": [
        "T26.1",
        "T26.2",
        "T26.3"
      ],
      "notes": "EXECUTE BEFORE T24. The docs validator currently enforces that docs/boundaries.md contains the fragment 'single-agent iteration/loop runner', which means the validator is a mechanism for locking in indecision rather than tracking a milestone. There is no acceptance criterion anywhere that would cause someone to say the deferral is lifted. T24 is the work that satisfies those criteria, so the criteria must be written before T24 runs — otherwise T24 has no done definition. Fixing this first also means T26.3 can validate that T24's tasks.json entries map to the documented criteria."
    },
    {
      "id": "T26.1",
      "title": "Create docs/multi-agent-readiness.md with explicit acceptance criteria mapped to T24's three gaps",
      "status": "blocked",
      "parentId": "T26",
      "notes": "Created ralph-codex-vscode-starter/docs/multi-agent-readiness.md with the required Task Ownership, Write Serialisation, Remediation Isolation, and Lifting The Deferral sections. The selected validation command still fails due to pre-existing absolute /home/admin/... links in the repository's existing docs, which the docs validator treats as outside-repo targets on this workspace.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs",
      "blocker": "Pre-existing docs validation failure: core docs use absolute /home/admin/Documents/repos/Ralph/ralph-codex-vscode-starter/... links, causing link_outside_repo and dependent missing_doc_map_entry errors during npm run check:docs."
    },
    {
      "id": "T26.2",
      "title": "Update docs/boundaries.md to reference the milestone doc instead of repeating the open-ended deferral",
      "status": "todo",
      "parentId": "T26",
      "dependsOn": [
        "T26.1"
      ],
      "notes": "In docs/boundaries.md under Control-Plane Boundary, replace the sentence 'Broad multi-agent orchestration stays deferred until nested workspace, inspection, execution, and verification root policy remains deterministic, test-backed, and evidence-backed.' with language that says the shipped control plane is a sequential single-agent loop and that multi-agent orchestration is a planned milestone with explicit acceptance criteria defined in docs/multi-agent-readiness.md. The fragment 'single-agent iteration/loop runner' must still appear in the file to keep the existing docsValidator rule passing — keep it in the sentence describing the current shipped state.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T26.3",
      "title": "Add docs/multi-agent-readiness.md to docsValidator required docs and wire a required-fragment check",
      "status": "todo",
      "parentId": "T26",
      "dependsOn": [
        "T26.2"
      ],
      "notes": "In src/validation/docsValidator.ts, add 'docs/multi-agent-readiness.md' to the REQUIRED_DOCS array and add a DOC_RULES entry requiring the headings 'Task Ownership', 'Write Serialisation', 'Remediation Isolation', and 'Lifting The Deferral', plus the required fragments 'acceptance criterion', 'claims.json', 'agentId', and 'npm run validate'. Add the doc to REQUIRED_AGENTS_DOC_MAP_TARGETS and REQUIRED_README_DOC_MAP_TARGETS so AGENTS.md and README.md must link to it. This makes the milestone criteria a continuous validation target, not a one-time editorial commitment.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T27",
      "title": "Decompose iterationEngine.ts by extracting completion-report parsing, reconciliation, and task decomposition into focused modules",
      "status": "todo",
      "dependsOn": [
        "T27.1",
        "T27.2",
        "T27.3",
        "T27.4"
      ],
      "notes": "EXECUTE BEFORE T24. iterationEngine.ts is 2050 lines and owns completion-report parsing, JSON extraction, reconciliation write logic, task decomposition proposal building, provenance bundle persistence, artifact cleanup, preflight coordination, loop orchestration, and the two public entry points. T24.2 (write serialisation) and T24.3 (remediation isolation) both require modifying this file. Extracting the highest-change-frequency concerns before T24 runs reduces the blast radius of those changes and makes the remaining iterationEngine.ts easier to reason about. The extraction must be purely structural — no behaviour changes — so existing tests prove correctness throughout."
    },
    {
      "id": "T27.1",
      "title": "Extract completion-report parsing into src/ralph/completionReportParser.ts",
      "status": "blocked",
      "parentId": "T27",
      "notes": "Extracted completion-report parsing from iterationEngine.ts into src/ralph/completionReportParser.ts and updated iterationEngine imports. TypeScript test compilation passed.",
      "validation": "cd ralph-codex-vscode-starter && npm test",
      "blocker": "Full npm test run is blocked by existing environment-level child-process failures: multiple tests fail with spawn EPERM when launching git or verifier commands."
    },
    {
      "id": "T27.2",
      "title": "Extract task-decomposition helpers into src/ralph/taskDecomposition.ts",
      "status": "todo",
      "parentId": "T27",
      "dependsOn": [
        "T27.1"
      ],
      "notes": "Move the following free functions from iterationEngine.ts into a new src/ralph/taskDecomposition.ts: normalizeWhitespace, decomposePhrase, isClearlyCompoundTask, deriveCompoundSegments, buildDecompositionProposal, remediationSuggestedChildTasks, normalizeRemediationForTask, remediationMatchesStopReason, remediationHistoryEntries, buildRemediationArtifact, remediationRationale, and remediationSummary. Export all of them. These functions collectively implement the bounded child-task proposal logic; grouping them makes the T24.3 agentId scoping change a single-file edit. Update iterationEngine.ts to import from the new module. Run npm test after the move.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T27.3",
      "title": "Extract reconcileCompletionReport into src/ralph/reconciliation.ts",
      "status": "todo",
      "parentId": "T27",
      "dependsOn": [
        "T27.2"
      ],
      "notes": "Move the private reconcileCompletionReport method and its CompletionReconciliationOutcome return type from RalphIterationEngine into a new standalone async function in src/ralph/reconciliation.ts. The function signature should take an explicit input object containing prepared context, selected task, verification status, preliminary classification, last message, task file path, and logger — removing the implicit dependency on the engine's private state. Update RalphIterationEngine to call the extracted function. This is the method that T24.2 will wrap with withTaskFileLock; extracting it first makes that wrapping a focused single-function change rather than a surgery inside a 2050-line class. Run npm test after the move.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T27.4",
      "title": "Add code-owner entries for the new modules and confirm iterationEngine.ts line count is materially reduced",
      "status": "todo",
      "parentId": "T27",
      "dependsOn": [
        "T27.3"
      ],
      "notes": "Add src/ralph/completionReportParser.ts, src/ralph/taskDecomposition.ts, and src/ralph/reconciliation.ts to the Code Owners For Behavior section of AGENTS.md with one-line descriptions. Update src/validation/docsValidator.ts REQUIRED_CODE_OWNER_FILES to include the three new modules. Confirm iterationEngine.ts is below 1200 lines after extraction — if it is not, identify which remaining concern is the largest candidate for a follow-on extraction and add a note to docs/architecture.md. Run npm run validate to confirm docs, types, and tests all pass.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate && wc -l src/ralph/iterationEngine.ts | awk '{if ($1 > 1200) exit 1}'"
    },
    {
      "id": "T28",
      "title": "Add a continuous automated ledger-integrity gate to npm run validate",
      "status": "todo",
      "dependsOn": [
        "T28.1",
        "T28.2",
        "T28.3"
      ],
      "notes": "EXECUTE AFTER T24. The done-task audit that caught T21's tracker drift was a manually-run one-off. Ralph shipped with a done parent and all-todo children — the exact condition T23 was written to detect — and no automated gate caught it before commit. T23 added preflight blocking at runtime, but preflight only runs when Ralph executes inside VS Code. A check:ledger script that validates tasks.json at npm run validate time would have caught T21 before it was committed. Placing this after T24 means the ledger check can also validate claim records once T24.1's claims.json schema exists."
    },
    {
      "id": "T28.1",
      "title": "Add scripts/check-ledger.js that validates tasks.json for drift, cycles, and orphaned parents",
      "status": "todo",
      "parentId": "T28",
      "notes": "Create ralph-codex-vscode-starter/scripts/check-ledger.js as a standalone Node.js script that reads .ralph/tasks.json relative to the workspace root (defaulting to the script's grandparent directory if no argument is given). It must detect and report: (1) done parents with any descendant in todo, in_progress, or blocked status (the T21 class of drift); (2) tasks whose dependsOn references a non-existent task id; (3) tasks whose parentId references a non-existent task id; (4) dependency cycles using a DFS traversal; (5) tasks in in_progress status with no corresponding entry in .ralph/claims.json once that file exists (skip this check if claims.json is absent, for backward compatibility). Exit code 0 on clean, 1 on any finding. Print each finding with the offending task id and a one-line description.",
      "validation": "node ralph-codex-vscode-starter/scripts/check-ledger.js && echo 'ledger clean'"
    },
    {
      "id": "T28.2",
      "title": "Wire check:ledger into package.json scripts and npm run validate",
      "status": "todo",
      "parentId": "T28",
      "dependsOn": [
        "T28.1"
      ],
      "notes": "Add a check:ledger script to package.json: 'node scripts/check-ledger.js'. Update the validate script to run check:ledger after check:docs and before the test suite so a drifted ledger blocks the full validation gate before running tests. Update docs/testing.md under Authoritative Commands to list npm run check:ledger and describe what it validates. Add the required fragment 'check:ledger' to the DOC_RULES entry for docs/testing.md in src/validation/docsValidator.ts.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:ledger && npm run check:docs"
    },
    {
      "id": "T28.3",
      "title": "Add regression tests for the ledger check script covering each drift class",
      "status": "todo",
      "parentId": "T28",
      "dependsOn": [
        "T28.2"
      ],
      "notes": "Add test/checkLedger.test.ts with test fixtures covering: a clean tasks.json exits 0; a done parent with a todo child exits 1 with the parent's id in the output; a missing dependsOn target exits 1; a missing parentId target exits 1; a dependency cycle exits 1; a valid in_progress task with a matching claims.json entry exits 0; a valid in_progress task with no claims.json present exits 0 (backward-compatible). Use child_process.spawnSync to invoke the script directly so the test covers the real exit-code contract rather than a mocked internal. Run npm run validate to confirm all gates pass.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T29",
      "title": "Surface the trust-chain epistemic gap explicitly in the provenance bundle and documentation",
      "status": "todo",
      "dependsOn": [
        "T29.1",
        "T29.2",
        "T29.3"
      ],
      "notes": "EXECUTE AFTER T24. The provenance bundle is presented as the flagship audit artifact, but it proves only that a specific string of text was handed to a black box. The completion-report.json artifact already captures the model's self-report, and reconciliationWarnings on RalphIterationResult already captures divergence between that self-report and what the verifier found — but neither is surfaced in the provenance bundle itself, and the documentation does not label the epistemic gap explicitly. In a multi-agent deployment this gap is more consequential: an audit trail that does not distinguish model claims from verifier evidence is harder to use for compliance purposes when multiple agents are producing interleaved artifacts."
    },
    {
      "id": "T29.1",
      "title": "Add completion-report divergence fields to RalphProvenanceBundle and persist them in the bundle artifact",
      "status": "todo",
      "parentId": "T29",
      "notes": "Add three optional fields to RalphProvenanceBundle in src/ralph/types.ts: completionReportStatus (RalphCompletionReportStatus | null), reconciliationWarnings (string[] | null), and completionReportPath (string | null). In src/ralph/artifactStore.ts, populate these fields when persisting the run-level provenance bundle: copy completionReportStatus and reconciliationWarnings from the RalphIterationResult, and set completionReportPath to the completion-report.json path in the iteration artifact directory if it exists. The bundle summary.md should include a one-line Model Self-Report line stating the completion report status and listing any reconciliation warnings. Add regression coverage in test/artifactStore.test.ts for a bundle with a rejected completion report containing reconciliation warnings.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T29.2",
      "title": "Add an Epistemic Gap section to docs/provenance.md documenting what the bundle cannot prove",
      "status": "todo",
      "parentId": "T29",
      "dependsOn": [
        "T29.1"
      ],
      "notes": "Add an Epistemic Gap section to docs/provenance.md between What Operators Can Verify and the existing closing content. The section must state explicitly: (1) the CLI provenance chain proves prompt integrity up to the codex exec boundary; (2) the completion report is the model's self-report and is labelled as unverified in the bundle — the field name completionReportStatus makes the epistemic status machine-readable; (3) reconciliationWarnings records cases where the model's claimed status diverged from what preflight and verifier evidence found; (4) the absence of reconciliation warnings does not prove model reasoning was correct — it proves the model's claimed status was consistent with the observable verifier signals; (5) operators requiring stronger guarantees should treat the verifier artifacts (validationCommand, gitDiff, taskState) as the authoritative evidence and treat the completion report as supplementary context.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T29.3",
      "title": "Add docsValidator fragment enforcement for the epistemic gap language",
      "status": "todo",
      "parentId": "T29",
      "dependsOn": [
        "T29.2"
      ],
      "notes": "Add requiredFragments to the DOC_RULES entry for docs/provenance.md in src/validation/docsValidator.ts: 'model self-report', 'unverified', 'reconciliationWarnings', and 'authoritative evidence'. Add 'Epistemic Gap' to the requiredHeadings list for docs/provenance.md. This ensures the epistemic gap documentation cannot be silently removed during future edits without breaking the validation gate. Run npm run validate to confirm.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T30",
      "title": "Document the prompt budget methodology and make policies model-configurable",
      "status": "todo",
      "dependsOn": [
        "T30.1",
        "T30.2",
        "T30.3"
      ],
      "notes": "EXECUTE AFTER T24. Every token target in the prompt budget policy matrix was derived empirically against Codex CLI with no documented methodology. If Codex CLI changes its context behaviour, or if Ralph is used with a different model (which T24 makes structurally possible), every budget number becomes wrong in ways that are silent — prompts will be trimmed according to a stale policy and the evidence records will show within budget against a target that was never re-validated. This task documents the derivation methodology, adds a calibration helper, and introduces a model-configurable budget profile so different adapters can carry different policies."
    },
    {
      "id": "T30.1",
      "title": "Create docs/prompt-calibration.md documenting how token targets were derived and how to re-derive them",
      "status": "todo",
      "parentId": "T30",
      "notes": "Create ralph-codex-vscode-starter/docs/prompt-calibration.md. It must document: (1) the target model and context window size that the current policy matrix was calibrated against; (2) the methodology used to arrive at each token target — specifically whether targets were set by observing real truncation behaviour, by estimating from section sizes, or by a different method; (3) a step-by-step recalibration procedure an operator could follow when switching to a different model or after a Codex CLI context-window change; (4) the mapping between ralphCodex.reasoningEffort values and their expected token overhead so operators can account for reasoning tokens when setting targets. Add the doc to REQUIRED_DOCS, REQUIRED_AGENTS_DOC_MAP_TARGETS, and REQUIRED_README_DOC_MAP_TARGETS in docsValidator.ts, and add a link in AGENTS.md and README.md.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T30.2",
      "title": "Add ralphCodex.promptBudgetProfile setting and wire it into promptBuilder's policy selection",
      "status": "todo",
      "parentId": "T30",
      "dependsOn": [
        "T30.1"
      ],
      "notes": "Add a ralphCodex.promptBudgetProfile string setting to package.json contributes.configuration with a default value of 'codex' and an enum of ['codex', 'claude', 'custom']. In src/config/types.ts add promptBudgetProfile to the config type. In src/prompt/promptBuilder.ts, make the policy matrix selection branch on promptBudgetProfile: 'codex' uses the current hardcoded targets; 'claude' uses a separate policy matrix with targets appropriate for Claude's longer context window (document the placeholder targets in docs/prompt-calibration.md as requiring calibration before production use); 'custom' reads token targets from a ralphCodex.customPromptBudget object setting. Add regression coverage in test/promptBuilder.test.ts for the claude profile selecting a different policy matrix than the codex profile.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T30.3",
      "title": "Add a budget calibration script that renders prompts across all kinds and reports estimated token counts",
      "status": "todo",
      "parentId": "T30",
      "dependsOn": [
        "T30.2"
      ],
      "notes": "Create ralph-codex-vscode-starter/scripts/calibrate-prompt-budget.js. The script should accept a workspace path argument, load a minimal tasks.json and prd.md from that workspace, render one prompt of each kind (bootstrap, iteration, fix-failure, continue-progress, replenish-backlog, human-review-handoff) for both cliExec and ideHandoff targets using the existing promptBuilder logic, and print a table showing: prompt kind, target, estimated token count, current budget target, and delta. The script is not part of npm run validate — it is a manual operator tool for recalibration. Document its usage in docs/prompt-calibration.md under the recalibration procedure. Add it to docs/testing.md under What Is Not Covered as a manual calibration tool.",
      "validation": "cd ralph-codex-vscode-starter && npm run check:docs"
    },
    {
      "id": "T31",
      "title": "Add prompt quality tests that assert on rendered content, not just section inclusion policy",
      "status": "todo",
      "dependsOn": [
        "T31.1",
        "T31.2",
        "T31.3"
      ],
      "notes": "EXECUTE LAST. The 801-line test/promptBuilder.test.ts verifies that sections are included or omitted according to the budget policy — zero assertions exist about whether the rendered text would cause a model to do the right thing. This is the most consequential untested output Ralph produces. This task is placed last because it benefits from: (1) the clean module boundaries from T27 making promptBuilder.ts easier to test in isolation; (2) the model-configurable budget profiles from T30 providing test dimensions; (3) T24's agentId fields giving the prompt fixture framework an additional variable to cover. Golden snapshot tests are the pragmatic first step — they do not prove prompt quality but they make regressions visible and force authors to consciously update snapshots when prompt content changes."
    },
    {
      "id": "T31.1",
      "title": "Build a prompt test fixture framework with representative workspace and task scenarios",
      "status": "todo",
      "parentId": "T31",
      "notes": "Create test/fixtures/promptScenarios.ts exporting a set of at least five named scenario objects, each containing: a minimal RalphTaskFile, a prd.md string, a progress.md string, a WorkspaceScan snapshot, and a prior iteration result (or null for bootstrap). The scenarios should cover: (1) a fresh workspace with no prior iterations; (2) a workspace mid-task with a prior partial_progress iteration; (3) a workspace with a repeated_no_progress stop and a pending decompose_task remediation; (4) a workspace with a blocked task; (5) a fix-failure scenario with a non-null validationFailureSignature. These fixtures are the inputs that T31.2 and T31.3 render prompts against. Keep each fixture minimal — the goal is controlled, reproducible inputs, not realistic project content.",
      "validation": "cd ralph-codex-vscode-starter && npm run compile"
    },
    {
      "id": "T31.2",
      "title": "Add golden snapshot tests for each prompt kind rendered against the fixture scenarios",
      "status": "todo",
      "parentId": "T31",
      "dependsOn": [
        "T31.1"
      ],
      "notes": "Add test/promptBuilder.snapshot.test.ts. For each combination of prompt kind and fixture scenario that is valid (e.g. fix-failure only against scenarios with a prior failed iteration), render the prompt using the existing promptBuilder logic and compare the output against a stored snapshot in test/fixtures/snapshots/. Use Jest's toMatchSnapshot or a custom snapshot mechanism that writes plain .md files so snapshot diffs are readable in code review. The first run should write the snapshots. Subsequent runs detect regressions. Add a note to docs/testing.md that snapshot updates require running npm test -- --updateSnapshot and that reviewers should read snapshot diffs as carefully as code diffs.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T31.3",
      "title": "Add content assertion tests for critical prompt sections across key scenarios",
      "status": "todo",
      "parentId": "T31",
      "dependsOn": [
        "T31.2"
      ],
      "notes": "Add content assertions to test/promptBuilder.test.ts (or a new test/promptBuilder.content.test.ts) that go beyond section-presence checks to assert on rendered text. At minimum: (1) a fix-failure cliExec prompt rendered against the repeated-failure fixture must contain the task's validation command string in the rendered text, not just assert that the execution section is included; (2) a human-review-handoff prompt rendered against the blocked fixture must contain the blocker text from the task; (3) a replenish-backlog prompt must contain the PRD objective text; (4) an iteration prompt rendered against the remediation fixture must contain a substring from the remediation summary; (5) a bootstrap prompt must not contain any prior-iteration context. These assertions are brittle by design — they should fail if the template rendering logic accidentally drops critical content under budget pressure.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T32",
      "title": "Auto-restart the extension host on control_plane_reload_required instead of waiting for operator action",
      "status": "todo",
      "dependsOn": [
        "T32.1",
        "T32.2",
        "T32.3"
      ],
      "notes": "EXECUTE AFTER T27 (iterationEngine decomposition), BEFORE T33. control_plane_reload_required is not an error — the iteration completed cleanly before the stop. The only reason a human is involved is that Ralph has no mechanism to trigger an extension host restart itself. VS Code exposes vscode.commands.executeCommand('workbench.action.reloadWindow') which restarts the extension host without closing the window. In loop mode this stop reason should auto-trigger that command after persisting the iteration result, rather than surfacing it as an operator decision point. This is the lowest-risk human-removal change in the codebase because the state is already clean when the reload fires."
    },
    {
      "id": "T32.1",
      "title": "Add ralphCodex.autoReloadOnControlPlaneChange boolean setting defaulting to false",
      "status": "todo",
      "parentId": "T32",
      "notes": "Add ralphCodex.autoReloadOnControlPlaneChange to package.json contributes.configuration as a boolean with default false and a description stating that when enabled, Ralph will automatically execute workbench.action.reloadWindow after a control_plane_reload_required stop in loop mode instead of waiting for operator action. Add the corresponding field to the config type in src/config/types.ts and the read path in src/config/readConfig.ts. Default is false so existing behaviour is unchanged until the operator explicitly opts in.",
      "validation": "cd ralph-codex-vscode-starter && npm run compile"
    },
    {
      "id": "T32.2",
      "title": "Wire auto-reload into the loop command handler when the stop reason is control_plane_reload_required",
      "status": "todo",
      "parentId": "T32",
      "dependsOn": [
        "T32.1"
      ],
      "notes": "In src/commands/registerCommands.ts, in the Run CLI Loop command handler, after an iteration returns with stopReason === 'control_plane_reload_required': if autoReloadOnControlPlaneChange is true, log a message to the output channel stating Ralph is reloading the extension host to apply control-plane changes, wait 1500ms to ensure the iteration result artifact has been flushed to disk, then call vscode.commands.executeCommand('workbench.action.reloadWindow'). The wait is intentional — do not skip it. If autoReloadOnControlPlaneChange is false, preserve the existing behaviour of surfacing the stop to the operator. Do not trigger auto-reload from the Run CLI Iteration single-shot command — only from the loop path.",
      "validation": "cd ralph-codex-vscode-starter && npm run compile"
    },
    {
      "id": "T32.3",
      "title": "Add regression coverage and document the auto-reload behaviour in docs/workflows.md",
      "status": "todo",
      "parentId": "T32",
      "dependsOn": [
        "T32.2"
      ],
      "notes": "Add tests in test/commandShell.smoke.test.ts covering: auto-reload is not triggered when autoReloadOnControlPlaneChange is false; auto-reload is not triggered from the single-shot iteration command even when the setting is true; the reload command is invoked with the correct VS Code command id when the setting is true and the stop reason is control_plane_reload_required. Mock vscode.commands.executeCommand in the test environment. Add a Control-Plane Reload subsection to the Run The Ralph Loop section of docs/workflows.md explaining the setting, the 1500ms flush delay, and that reloadWindow is only safe here because the iteration result was already persisted before the reload fires. Run npm run validate.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T33",
      "title": "Auto-apply decompose_task and mark_blocked remediation actions without operator intervention",
      "status": "todo",
      "dependsOn": [
        "T33.1",
        "T33.2",
        "T33.3",
        "T33.4"
      ],
      "notes": "EXECUTE AFTER T28 (ledger gate) and T24.1 (task claiming). The manual Apply Latest Task Decomposition Proposal command exists because writing to tasks.json felt consequential at the time. But buildRemediationArtifact already validates proposals at construction: child tasks are one level deep, capped at three, inherit only the parent validation command, and are rejected at write time if they create duplicates or graph invalidity. The human in this loop is not adding judgment — they are running a command that Ralph could run itself. decompose_task auto-application is safe under those hard constraints. mark_blocked is safer still — it is additive, setting a status and blocker field without restructuring the graph. reframe_task is explicitly excluded from auto-application because it changes the semantic meaning of a task title and scope, which requires human intent. request_human_review is a hard stop by definition and must never be auto-applied."
    },
    {
      "id": "T33.1",
      "title": "Add ralphCodex.autoApplyRemediation setting with decompose_task and mark_blocked as opt-in actions",
      "status": "todo",
      "parentId": "T33",
      "notes": "Add ralphCodex.autoApplyRemediation to package.json contributes.configuration as an array of strings with items enum ['decompose_task', 'mark_blocked'] and default empty array. This means auto-application is fully opt-in — an operator who wants both actions automated sets ['decompose_task', 'mark_blocked']; an operator who only wants mark_blocked sets ['mark_blocked']; the default empty array preserves current behaviour exactly. Do not include reframe_task or request_human_review in the enum — they must not be auto-applicable. Add the field to src/config/types.ts and src/config/readConfig.ts.",
      "validation": "cd ralph-codex-vscode-starter && npm run compile"
    },
    {
      "id": "T33.2",
      "title": "Implement auto-application of mark_blocked in the iteration engine after a blocked stop",
      "status": "todo",
      "parentId": "T33",
      "dependsOn": [
        "T33.1",
        "T24.1.3"
      ],
      "notes": "In src/ralph/iterationEngine.ts (or src/ralph/reconciliation.ts after T27.3), after the iteration result is classified and persisted with stopReason === 'repeated_identical_failure' and remediation.action === 'mark_blocked': if 'mark_blocked' is in autoApplyRemediation, acquire the task file lock via withTaskFileLock (T24.2.1), read the current tasks.json, set the selected task status to 'blocked' and populate the blocker field with remediation.summary, write the file, release the lock, and log to the output channel that mark_blocked was auto-applied with the task id and blocker text. Record the auto-application in the iteration result warnings array with the text 'Remediation auto-applied: mark_blocked on task <id>' so the provenance bundle reflects what happened. Run npm test.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T33.3",
      "title": "Implement auto-application of decompose_task using the existing apply-proposal write path",
      "status": "todo",
      "parentId": "T33",
      "dependsOn": [
        "T33.2"
      ],
      "notes": "In src/ralph/iterationEngine.ts, after the iteration result is classified and persisted with remediation.action === 'decompose_task' and suggestedChildTasks is non-empty: if 'decompose_task' is in autoApplyRemediation, call the same validated write function used by the Apply Latest Task Decomposition Proposal command — do not duplicate the write logic. The existing write path already: validates the parent task exists and is not done, rejects duplicate child ids, rejects graph-invalid proposed dependencies, and appends children atomically. Wrap the call in withTaskFileLock. Record the auto-application in the iteration result warnings array with 'Remediation auto-applied: decompose_task on task <id>, added <n> child tasks'. Run npm test.",
      "validation": "cd ralph-codex-vscode-starter && npm test"
    },
    {
      "id": "T33.4",
      "title": "Add regression coverage for auto-remediation and document the behaviour in docs/verifier.md and docs/workflows.md",
      "status": "todo",
      "parentId": "T33",
      "dependsOn": [
        "T33.3"
      ],
      "notes": "Add tests in test/iterationEngine.integration.test.ts covering: mark_blocked is auto-applied when the action matches and the setting includes it; decompose_task is auto-applied and the resulting tasks.json contains the expected child tasks; neither action is applied when the setting is empty; reframe_task is never auto-applied regardless of setting; request_human_review is never auto-applied regardless of setting; auto-application is recorded in the iteration result warnings. In docs/verifier.md under Task Remediation, add a paragraph describing the autoApplyRemediation setting, which actions are eligible, and why reframe_task and request_human_review are excluded. Add a brief note to docs/workflows.md under Run The Ralph Loop. Run npm run validate.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T34",
      "title": "Automatically continue into replenish-backlog prompt when backlog is cleanly exhausted in loop mode",
      "status": "todo",
      "dependsOn": [
        "T34.1",
        "T34.2"
      ],
      "notes": "EXECUTE AFTER T28 (ledger gate). When the durable backlog is cleanly exhausted Ralph already selects the replenish-backlog prompt kind — but the loop currently stops with no_actionable_task before executing it, requiring the operator to manually rerun. If the ledger is consistent (no drift detected by T28's check-ledger gate) and the stop reason is no_actionable_task with backlog.actionableTaskAvailable === false, the loop can safely continue into the replenishment iteration automatically. The replenishment prompt is deterministically generated and the resulting tasks.json still gets implicit human review when the operator decides whether to continue the next loop. The ledger gate from T28 is the safety dependency — auto-continuing into replenishment is only safe when the ledger is provably consistent, not when the exhaustion is a false positive caused by drift."
    },
    {
      "id": "T34.1",
      "title": "Add ralphCodex.autoReplenishBacklog boolean setting and wire it into the loop stop decision",
      "status": "todo",
      "parentId": "T34",
      "notes": "Add ralphCodex.autoReplenishBacklog boolean to package.json contributes.configuration with default false. In src/ralph/loopLogic.ts, in the shouldContinue decision path: when stopReason is no_actionable_task and the iteration result backlog shows actionableTaskAvailable === false and no ledger drift was detected in preflight (preflight.diagnostics has no error-severity finding with code matching 'ledger_drift' or 'done_parent_unfinished_descendants'), and autoReplenishBacklog is true, return shouldContinue: true with a message indicating the loop is continuing into backlog replenishment. The ledger-drift check is the critical safety gate — if drift is present, do not auto-continue regardless of the setting. Add the field to src/config/types.ts and src/config/readConfig.ts.",
      "validation": "cd ralph-codex-vscode-starter && npm run compile"
    },
    {
      "id": "T34.2",
      "title": "Add regression coverage and document auto-replenishment in docs/workflows.md",
      "status": "todo",
      "parentId": "T34",
      "dependsOn": [
        "T34.1"
      ],
      "notes": "Add tests in test/loopLogic.test.ts covering: auto-replenishment continues the loop when the backlog is clean and the setting is true; auto-replenishment does not continue when ledger drift is present in preflight diagnostics even if the setting is true; auto-replenishment does not continue when autoReplenishBacklog is false; the loop still stops when no_actionable_task is accompanied by an error-severity ledger-drift diagnostic regardless of the setting. In docs/workflows.md under Run The Ralph Loop, add a Backlog Replenishment subsection explaining that auto-replenishment is safe only when the ledger is consistent, why the drift check is the safety gate, and how to use the setting. Run npm run validate.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    },
    {
      "id": "T35",
      "title": "Add a fully-autonomous loop mode that composes T32, T33, and T34 with explicit hard stops preserved",
      "status": "todo",
      "dependsOn": [
        "T35.1",
        "T35.2"
      ],
      "notes": "EXECUTE LAST, after T32, T33, and T34 are all complete. T32 (auto-reload), T33 (auto-remediation), and T34 (auto-replenishment) each introduce an individual opt-in setting. An operator who wants maximum autonomy should not need to set three separate flags. A ralphCodex.autonomyMode: 'supervised' | 'autonomous' setting that implicitly enables all three when set to 'autonomous' is cleaner to operate and makes the intent explicit. Critically, autonomous mode must document and enforce the hard stops that remain: needs_human_review and request_human_review are non-negotiable halts; the initial PRD must be human-authored; autonomous mode does not bypass preflight blocking. The hard stops are not configurable."
    },
    {
      "id": "T35.1",
      "title": "Add ralphCodex.autonomyMode setting and wire it as a shorthand for the three individual autonomy settings",
      "status": "todo",
      "parentId": "T35",
      "dependsOn": [
        "T32.1",
        "T33.1",
        "T34.1"
      ],
      "notes": "Add ralphCodex.autonomyMode to package.json contributes.configuration as a string enum with values 'supervised' (default) and 'autonomous'. In src/config/readConfig.ts, after reading individual settings, apply the override: if autonomyMode === 'autonomous', set autoReloadOnControlPlaneChange to true, autoApplyRemediation to ['decompose_task', 'mark_blocked'], and autoReplenishBacklog to true, regardless of their individual values. If autonomyMode === 'supervised', individual settings are used as-is, allowing mixed configurations. Log the effective autonomy configuration to the output channel at extension activation so the operator can confirm what mode is active. Add the field to src/config/types.ts.",
      "validation": "cd ralph-codex-vscode-starter && npm run compile"
    },
    {
      "id": "T35.2",
      "title": "Document autonomous mode, its hard stops, and the trust model in docs/boundaries.md and docs/workflows.md",
      "status": "todo",
      "parentId": "T35",
      "dependsOn": [
        "T35.1"
      ],
      "notes": "Add an Autonomous Loop Mode section to docs/workflows.md covering: what autonomyMode: autonomous enables; the hard stops that are never automated (needs_human_review, request_human_review, initial PRD authorship); the safety dependencies (T28 ledger gate must be passing, T24 claim ownership must be active for multi-agent use); a recommended pre-flight checklist before enabling autonomous mode (clean ledger, human-reviewed tasks.json, validation command confirmed executable). Add a brief paragraph to docs/boundaries.md under Control-Plane Boundary noting that autonomous mode does not change the principal-agent model — the operator remains the principal and the hard stops are enforced regardless of mode. Add 'autonomyMode' and 'hard stops' as required fragments to the DOC_RULES entry for docs/boundaries.md in docsValidator.ts. Add regression coverage in test/commandShell.smoke.test.ts confirming that needs_human_review stops the loop even when autonomyMode is autonomous. Run npm run validate.",
      "validation": "cd ralph-codex-vscode-starter && npm run validate"
    }
  ]
}
