# Progress

- Ralph workspace initialized.
- Refined `.ralph/prd.md` into a concrete objective for a durable, file-backed multi-agent delivery framework spanning requirements, implementation, testing, and orchestration.
- Replaced the seed backlog in `.ralph/tasks.json` with repo-specific work derived from the actual `ralph-codex-vscode-starter` extension surface and current coverage gaps.
- Completed T3 by validating nested project-root selection for `ralph-codex-vscode-starter`, adding scanner/status regressions, and rebuilding the extension runtime artifacts.
- Completed T4 by adding regression coverage for nested workspace root-selection candidates and prompt repo-context rendering so nested child repos remain visible in generated prompts.
- Completed T5 by adding command-shell smoke coverage for prompt clipboard auto-copy and Open Codex IDE handoff modes, and by documenting the exact clipboard, IDE-command, fallback, and live-host testing boundaries.
- Completed T6 by documenting the manual `.vsix` install workflow, making `npm run package` succeed with `vsce package --no-dependencies` plus repository-aware README links, and validating a fresh `ralph-codex-workbench-0.1.0.vsix` build while capturing the remaining packaging warnings.
- Completed T7 by adding an optional real `codex exec` temp-workspace smoke command, documenting its environment-sensitive usage, and surfacing summarized execution failure messages into the structured Ralph iteration/latest-result/status artifacts after a sandboxed real-run preserved backend-connect failure evidence.
- Advanced T8 by persisting explicit workspace/inspection/execution/verification root policy in prompt evidence, execution plans, CLI invocation artifacts, iteration results, provenance bundles, and status summaries; `codex exec` plus validation/git verifiers now run from the selected inspection root while `.ralph` remains anchored at the workspace root, with nested-root regressions and focused docs updated. Manual override for ambiguous umbrella workspaces is still pending under T8.4.
- Completed T8 by adding the `ralphCodex.inspectionRootOverride` escape hatch for ambiguous umbrella workspaces, validating override paths against the workspace boundary, surfacing applied or invalid override state in scan evidence and status output, expanding override-focused regression coverage, and updating the nested-root docs to reflect the now-complete root policy.
- Completed T9 by locking the shipped automation scope to a sequential single-agent loop, documenting that broader multi-agent orchestration remains deferred until nested root semantics are deterministic and evidence-backed, and adding docs-validation coverage so the guardrail is test-backed.
- Use this file for durable progress notes between fresh Codex runs.
- Fixed the verifier normalization bug that left legacy `cd ralph-codex-vscode-starter && npm run validate` hints unnormalized when the repo itself was the opened workspace. Full `npm run validate` now passes again, but the retention task itself is not yet complete.
- Surfaced generated artifact retention in Show Status, added explicit enabled/disabled coverage, and aligned testing docs with the existing prompt/run/iteration cleanup behavior.
- Made generated-artifact cleanup honor an explicit allowlist of protected state/latest references and added regression coverage so unrelated path-like fields do not preserve old prompts, run artifacts, or iteration directories.
- Made generated-artifact retention precedence explicit as newest-N plus protected references, added regression coverage proving older protected entries remain retained in newest-first order, and aligned the retention docs with that rule.
- Added iteration-engine regression coverage proving blocked-preflight cleanup preserves the prior state-referenced prompt, transcript/last-message pair, and iteration artifact directory when generated-artifact retention is 1.
- Protected generated-artifact cleanup against deleting iteration files that are still referenced by latest-provenance-bundle.json or latest-provenance-failure.json, and added regression coverage for that retention case.
- Aligned the durable retention docs with the implemented behavior: generated-artifact cleanup now explicitly documents protection for paths still referenced by latest provenance bundle/failure pointers, and the testing doc now records that regression coverage.
- Protected generated prompts, run transcripts/last-message files, and iteration directories that are referenced only through durable state runHistory and iterationHistory, with regression and integration coverage.
- Aligned the retention invariants, workflow docs, and testing docs with the implemented generated-artifact protection behavior so `.ralph/state.json` history references (`runHistory[]` and `iterationHistory[]`) are explicitly treated as protected roots alongside `lastRun` and `lastIteration`.
- Extended generated-artifact protection so latest-prompt-evidence.json now protects the implied prompt file and iteration directory, added a regression proving it does not protect unrelated run artifacts, and updated retention docs to enumerate that protected-root rule.
- Added integration coverage proving generated-artifact cleanup preserves an older blocked integrity iteration when latest-provenance-failure.json is the remaining protected reference and retention is 1.
- Extended generated-artifact cleanup so run-only workspace state still protects the corresponding iteration directory, prompt, and transcript/last-message pair, then added regression coverage and doc updates for that fallback behavior.
- Protected prompt/transcript/last-message artifacts from raw state run references even when iteration is missing, added regression coverage for that boundary, and aligned invariants/testing docs.
- Added regression coverage and invariant/testing docs proving generated-artifact cleanup preserves older prompt/run/iteration artifacts when they are referenced only by latest-execution-plan.json or latest-cli-invocation.json. Validation passed with npm test.
- Extended generated-artifact retention so latest-result.json now protects referenced prompt and transcript/last-message artifacts, added regression and integration coverage, and aligned retention docs.
- Closed a remaining generated-artifact retention gap by deriving protected iteration references from lastRun when iterationHistory is absent, and added regression coverage for the lastRun-only state shape.
- Added explicit regression and doc coverage proving latest-preflight-report.json protects only the referenced iteration directory during generated-artifact cleanup; npm test passed.
- Made generated-artifact retention precedence explicit in the high-level README and architecture docs: Ralph keeps the newest parsed-iteration window first, then adds older protected references without evicting newer retained entries.
- Added focused artifact-retention regressions for latest-linked prompt/summary/preflight references and state-referenced transcript, last-message, and iteration-directory roots; updated testing docs accordingly.
- Added deterministic repair for deleted latest summary/provenance summary surfaces from surviving latest JSON metadata, wired status/open-latest flows through that repair path, and covered manual-deletion regressions.
- Expanded Show Status with recent iteration/run history, live generated-artifact and provenance-bundle retention summaries, and latest-artifact repair/stale reporting; added rendering tests and aligned workflow/testing docs.
- Added direct commands to open the latest prompt-evidence artifact and the latest CLI transcript with last-message fallback, plus registration/docs/smoke coverage.
- Added preflight artifact-readiness diagnostics for stale latest surfaces, missing latest-pointer targets, unsafe retention-root overlap, and disabled retention settings; wired them into iteration-start and status preflight paths; added regression coverage and doc updates.
- Added a scoped Cleanup Runtime Artifacts command that preserves durable Ralph state and latest evidence while pruning older generated prompts, run artifacts, iteration directories, provenance bundles, and logs; added regression coverage and updated docs.
- Hardened latest-artifact command recovery by making Open Latest CLI Transcript fall back to the persisted last-message artifact when latest CLI metadata points at a deleted transcript file, and added command-shell regression coverage. Validation passed with npm run validate.
- Fixed stale latest-cli-invocation fallback so Open Latest CLI Transcript now falls through to existing state-backed transcript or last-message artifacts when the latest invocation paths were manually deleted, and added command-shell regression coverage. Validation passed with npm run validate.
- Aligned AGENTS.md with the new latest-artifact and cleanup command surface added for T18; npm run validate passes cleanly on the current worktree.
- Removed the remaining vsce packaging warnings by adding a bundled MIT LICENSE file and a package.json files allowlist, updated release-build docs to describe the curated VSIX payload, and revalidated with npm run package plus npm run check:docs.
- Added a bounded, deterministic task-remediation recommendation for repeated no-progress and repeated identical failure outcomes, surfaced it in iteration/state artifacts and status/prompt context, and covered it with tests and docs.
- Implemented bounded structured child-task proposals for repeated no-progress remediation artifacts, surfaced proposal details in status output, updated docs, and validated with npm run validate.
- Added an explicit operator-approved command that applies the latest accepted decomposition proposal into .ralph/tasks.json while keeping remediation defaulted to propose-only.
- Added deterministic prompt-budget policy, task-aware prompt context trimming, and prompt-budget evidence persistence for CLI and IDE handoff flows; added regression coverage and verifier docs.
- Advanced T22 by implementing the T22.5 slice: added a default-medium `ralphCodex.reasoningEffort` setting, threaded it into `codex exec` as `model_reasoning_effort`, persisted the chosen value in transcript/invocation/integrity artifacts, and added regression coverage plus docs.
- Made prior-iteration prompt context task-aware so fix-failure and continue-progress prompts keep relevant remediation and validation signals under budget while omitting unrelated prior noise. Added regression coverage and passed full validation.
- Documented the prompt-budget policy, omission order, promptBudget evidence fields, and quota-saving reasoning-effort guidance in the focused workflow/provenance docs while preserving the existing validated implementation.
- Advanced T22 by tightening task-aware repo-context selection so prompt/CLI-focused tasks keep source-root context, and added regression coverage to keep budgeted prompt rendering aligned with code-facing prompt work.
- Advanced T22 by surfacing latest prompt-budget and reasoning-effort evidence in Show Status, with regression coverage and workflow docs so quota-sensitive CLI and IDE handoff behavior is inspectable from the primary status surface.
- Added explicit prompt-budget outcome accounting so prompt evidence and Show Status now report whether the final prompt stayed within target and by how many estimated tokens it was under or over; updated regression coverage and docs, and `npm run validate` passed.
- Allowed oversized continue-progress/iteration CLI prompts to omit progressContext after runtime and repo trimming, and added regression coverage proving the CLI budget path can stay within target by dropping recent progress when needed.
- Added regression coverage for token-budgeted human-review CLI prompts so blocker/remediation context is preserved ahead of progress trimming, and aligned workflow docs with that omission priority.
- Tightened prompt repo-context selection so generic cliExec prompts no longer include package/test inventory by default, while code-focused tasks still keep source and validation context; added regression coverage for code-focused and docs-focused CLI prompts and updated workflow docs.
- Extended prompt-budget evidence and status reporting to expose each prompt policy's required sections and fixed omission order, with docs and regression coverage; validation passed via npm run validate.
- Extended Show Status to display prompt-budget optional sections alongside required, omission-order, selected, and omitted sections; added regression coverage and aligned workflow documentation. Validation passed with npm run validate.
- Documented that bounded decomposition should start by reproducing the blocker against the inherited validation command, and added docs-validator coverage so the deterministic verification-target rule stays enforced.
- Resynced `.ralph/tasks.json` to the current worktree after repeated no-progress retries showed ledger drift: the stuck `T21.6.1.2` remediation-doc slice was already documented, the implemented `T22` prompt-budget work was still marked incomplete, and the backlog was corrected so Ralph will stop selecting stale tasks.
- Replenished the exhausted durable backlog with a new T23 slice focused on task-ledger integrity: detect done parents with unfinished descendants, surface tracker-drift warnings in preflight/status/backlog-replenishment context, add regression coverage, and document the operator repair path so future backlog exhaustion is less likely to come from bookkeeping drift.
- Surfaced task-ledger drift explicitly in preflight summaries, status output, and replenish-backlog prompt selection/strategy so inconsistent ledgers no longer read as clean backlog exhaustion. Added regression coverage for the new wording and prompt decision path.
- Re-inspected durable repo state and confirmed T23.1 is already implemented: done parents with unfinished descendants are reported as task-ledger drift in task-file inspection, surfaced through preflight/status consumers, and covered by regression tests. No code edits were necessary.
- Added regression coverage for done-parent ledger-drift diagnostics, exhausted-backlog loop stopping, drift-specific preflight blocking, and status rendering.
- Documented task-ledger integrity rules, explicit drift warnings, and the operator repair-vs-replenish decision path in the invariant and workflow docs. Validation passed with npm run check:docs.
- Auto-completed satisfied aggregate parent tasks during completion-report reconciliation so child-complete ledger drift no longer leaves stale backlog items behind.
- Added RalphTaskClaimStatus, RalphTaskClaim, and RalphTaskClaimFile to src/ralph/types.ts for a flat, inspectable task-claim schema consistent with tasks.json.
- Added optional agentId to RalphIterationResult and RalphRunRecord, defaulted legacy/missing records to the 'default' sentinel in state normalization, and persisted the sentinel on newly created single-agent iteration/run records. Compile passed.
- Updated .ralph/.gitignore to exclude /state.json and /done-task-audit*.md. Could not remove tracked runtime files from the Git index because the sandbox user is denied write access to .git, so .ralph/state.json, .ralph/done-task-audit-current-worktree.md, and .ralph/logs/extension.log remain tracked.
- Created ralph-codex-vscode-starter/docs/multi-agent-readiness.md with the required Task Ownership, Write Serialisation, Remediation Isolation, and Lifting The Deferral sections. The selected validation command still fails due to pre-existing absolute /home/admin/... links in the repository's existing docs, which the docs validator treats as outside-repo targets on this workspace.
- Extracted completion-report parsing from iterationEngine.ts into src/ralph/completionReportParser.ts and updated iterationEngine imports. TypeScript test compilation passed.
- Removed the remaining tracked Ralph runtime artifacts from the Git index now that .git was writable in this run. Validation confirms only prd.md, tasks.json, progress.md, and .gitignore remain tracked under .ralph.
- Added Ralph Codex: Initialize Workspace to scaffold a fresh .ralph directory, documented the flow, and added smoke coverage for create and abort behavior.
- Documented the committed project-artifact vs operator-local runtime-state boundary in docs/boundaries.md and the root README, and updated docsValidator to require the new wording.
- Promoted docs/multi-agent-readiness.md into the durable documentation map, linked it from the single-agent boundary, and confirmed the docs validator passes.
- Added focused unit coverage for the extracted completion-report parser and cleaned the iteration engine import to depend only on the parser entrypoint.
- Added completion-report divergence fields to RalphProvenanceBundle, persisted them from executed iteration results including completion-report.json when present, added Model Self-Report summary output, and covered the rejected-report reconciliation case with regression tests.
- Added ralphCodex.autoReplenishBacklog config plumbing and updated loop stop logic to continue into backlog replenishment only when no actionable task remains, backlog.actionableTaskAvailable is false, and preflight shows no ledger-drift safety errors. Added loop logic coverage for the enabled path and the ledger-drift gate.
- Updated the Control-Plane Boundary wording to describe the shipped state as a sequential single-agent iteration/loop runner and kept the docs validator fixture aligned with that wording.
- Updated the Control-Plane Boundary sentence to describe the current shipped state while retaining the required 'single-agent iteration/loop runner' fragment, and docs validation passed.
- Updated docs/boundaries.md so the Control-Plane Boundary states the shipped control plane is a sequential single-agent iteration/loop runner and points to docs/multi-agent-readiness.md for the explicit acceptance criteria for future multi-agent orchestration. Documentation validation passed.
- Added docs/multi-agent-readiness.md to required docs, AGENTS/README doc-map targets, and DOC_RULES with the required headings and fragments. Ran npm run check:docs successfully.
- Confirmed completion-report parsing is already extracted into src/ralph/completionReportParser.ts and repaired the stale docs-validator fixture so the full test suite passes again.
- Extracted task decomposition and remediation helper functions from iterationEngine.ts into src/ralph/taskDecomposition.ts, updated iterationEngine imports, and verified with npm test.
- Extracted completion-report reconciliation from RalphIterationEngine into src/ralph/reconciliation.ts with explicit inputs and updated the engine to call the standalone helper.
- Verified that the code-owner entries, docs-validator updates, and architecture follow-on note are already present. `npm run validate` passes, but the task validation still fails because `src/ralph/iterationEngine.ts` is 1450 lines, so the remaining work is another extraction focused on the provenance-bundle and prepared-context assembly path.
- Extracted prompt/preflight/prepared-context assembly from iterationEngine.ts into iterationPreparation.ts, updated shared PreparedIterationContext imports, and reduced iterationEngine.ts to 1073 lines while keeping full validation green.
- Aligned repeated-failure remediation counting with the shared agent-scoped trailing-history helper in loopLogic.ts so interleaved history from another agent cannot accumulate into the same-task streak logic.
- Added regression coverage for per-agent repeated-no-progress isolation, preserved default single-agent behavior coverage, and documented agentId-scoped repeated-stop detection in verifier docs.
- Added dedicated regression coverage proving explicit agent-scoped remediation and stop detection do not inherit default-agent history, and verified with npm test.
- Hardened claim acquisition so an already-contested ledger returns contested even when the canonical latest claim matches the caller, and added regression coverage. Validation passed with `cd ralph-codex-vscode-starter && npm test`.
- Added regression coverage proving releaseClaim uses file-level locking under concurrent release attempts, with one released outcome, one not_held outcome, and a single released ledger record. Validation passed with npm test (227 passed).
- Added regression coverage proving concurrent releaseClaim calls serialize through the file lock and documented the expanded claim-lock coverage. Validation passed with `cd ralph-codex-vscode-starter && npm test`.
- Gated task selection and completion-report reconciliation on durable claim ownership, added claim_contested stop handling, and released claims after each iteration with regression coverage.
- Added a file-backed tasks.json write lock helper with retry/backoff timeout semantics, wired current task-file mutation paths through it, and added concurrency and timeout regression coverage.
- Added claim-graph inspection plus preflight and Show Status surfacing for contested, stale, and provenance-mismatched task claims; updated docs and regression coverage.
- Removed the validation blocker by adding a test-only process runner harness and fake git baseline so claim-lifecycle and handoff-path coverage now validates cleanly under sandboxed execution. Full `npm run validate` passes.
- Added an explicit operator-approved stale-claim recovery command that atomically marks canonical stale claims in claims.json, records durable resolution metadata, and surfaces the resolved task/provenance/reason in status and preflight reporting. Added regression coverage and updated operator docs.
- Added regression coverage for stale-claim recovery returning a task to CLI reselection and explicit status/doc wording for the claim lifecycle across CLI execution and IDE prompt preparation.
- Wrapped initializeFreshWorkspace tasks.json write in withTaskFileLock; all other mutation paths (reconciliation, stateManager.inspectTaskFile, stateManager.updateTaskFile) were already locked.
- Added lock-cleanup-on-throw regression test to taskFile.test.ts and documented the write-serialisation contract (lock path, hold duration, timeout, process-exit behaviour) in docs/invariants.md under a new Task Graph Write Serialisation section. All 268 tests pass.
- Serialised all tasks.json mutation paths (reconciliation, workspace seed) behind withTaskFileLock; added cleanup and concurrency regression tests; all 268 tests pass
- Lifted multi-agent deferral: updated boundaries.md, multi-agent-readiness.md, and invariants.md to record milestone satisfied 2026-03-17; relaxed single-agent fragment check in docsValidator; all 271 tests pass.
- Added Epistemic Gap section to docs/provenance.md covering the five required points: CLI provenance boundary, completionReportStatus machine-readable label, reconciliationWarnings semantics, absence-of-warnings meaning, and operator guidance to treat verifier artifacts as authoritative.
- markTaskInProgress atomically promotes todo→in_progress at claim time; beforeCoreState re-captured after selectClaimedTask so the bookkeeping change is not counted as agent progress; 285 tests pass, validate exits 0.
- Completed the remaining T36 stale-state preflight slice by making checkStaleState claim-specific. Concurrent agents no longer mask stale-claim warnings with unrelated iteration results or state timestamps; added regression coverage and aligned invariants.
- Added dedicated Agent Health rendering to Show Status and covered it with status-report tests. Full validate ran; new coverage passed, but an existing unrelated concurrent runCliIteration state.lock timeout still fails the suite.
- Verified durable repo state already satisfies T28.2: package.json exposes check:ledger and validate runs check:docs -> check:ledger before tests; docs/testing.md documents check:ledger under Authoritative Commands; docsValidator requires the check:ledger fragment for docs/testing.md. Ran npm run check:ledger and npm run check:docs successfully.
- No code change was necessary because docsValidator already enforces the Epistemic Gap heading and required provenance fragments for docs/provenance.md. Ran npm run validate: documentation validation passed, but the overall suite failed on unrelated existing checkLedger exit-code tests and one iterationEngine EPERM temp-lock test.
- Completed prompt calibration documentation and clarified that the documented baseline is tied to the checked-in runtime defaults in src/config/defaults.ts; docs validation passes.
- Existing worktree changes already add the required check-ledger regression coverage in test/checkLedger.test.ts using child_process.spawnSync against scripts/check-ledger.js. No further task-specific edit was needed.
- Surfaced the epistemic gap directly in executed provenance bundles via explicit model-claim vs verifier-evidence fields and summary sections, updated provenance/invariant docs, and tightened docs-validation and artifact-store regression coverage.
- Added regression coverage for auto-replenishment safety gates in loopLogic tests and documented backlog auto-replenishment safety and drift gating in docs/workflows.md. Task-specific loopLogic test passes; full validate still fails on the existing unrelated iterationEngine EPERM temp-lock test.
- Confirmed the promptBudgetProfile setting, customPromptBudget wiring, Claude profile selection, and regression coverage were already present; added the remaining workflow-doc clarification that the published matrix is the calibrated codex baseline and reran validation successfully.
- Fixed auto-replenish loop continuation to use clean backlog exhaustion instead of an unset stopReason, and added regressions for clean exhaustion vs blocked/drift cases.
- Inspected the durable state and confirmed T37.1 was already implemented: ralphCodex.agentId is configured and threaded through claim acquisition, run/iteration records, preflight default-agent collision warnings, and durable .ralph/agents/<agentId>.json history. Validation output showed compile/check-docs/check-ledger/lint/tests succeeded with 289 passing tests and 0 failures.
- Added scripts/calibrate-prompt-budget.js and documented the manual prompt-budget recalibration workflow in prompt-calibration.md and testing.md.
- Added a standard npm entry point for prompt-budget calibration and aligned README/testing/calibration docs around the model-configurable budget workflow. Full validation is currently blocked by a pre-existing TypeScript compile error in src/ralph/artifactStore.ts requiring activeClaimSummary on a persisted preflight report.
- Grouped active claims by agentId in preflight and Show Status, persisted the summary into preflight artifacts, added regression coverage for two-agent rendering and stale state, and documented multi-agent agentId/status usage.
- Added ralphCodex.autoReloadOnControlPlaneChange as a boolean setting defaulting to false in package.json, src/config/types.ts, src/config/defaults.ts, and src/config/readConfig.ts so the new control-plane reload toggle is available through the typed config surface.
- Added ralphCodex.autoApplyRemediation as an opt-in array setting with only decompose_task and mark_blocked allowed, defaulted it to empty array, and threaded it through package.json, config types, defaults, and config loading. Also aligned the existing autoReloadOnControlPlaneChange field in typed/default config so compile stays consistent.
- Added deterministic prompt scenario fixtures and a guard test covering bootstrap, partial-progress, repeated-no-progress remediation, blocked-task, and validation-failure cases.
- Wired loop-only auto-reload for control_plane_reload_required in registerCommands.ts. When autoReloadOnControlPlaneChange is true, Ralph logs the reload message, waits 1500ms for artifact flush, and executes workbench.action.reloadWindow; otherwise the existing operator-facing stop message remains unchanged.
- Added ralphCodex.autonomyMode as a supervised/autonomous shorthand, applied the effective override in readConfig, logged the resolved autonomy configuration at activation, updated the workflow doc, and added focused config plus activation tests.
- Added readable golden snapshot coverage for each prompt kind, expanded fixture scenarios to include replenish-backlog and human-review-handoff, generated plain .md snapshots, and documented npm test -- --updateSnapshot for snapshot refreshes. Validation remains red only because of unrelated pre-existing test failures in stateManager.test and iterationEngine.integration.test.
- Implemented automatic mark_blocked remediation application in the iteration engine using withTaskFileLock, persisted the required warning text in iteration results, logged the auto-apply event, and added an integration test covering the repeated blocked stop path.
- Persisted agentId into durable preflight, execution-plan, CLI-invocation, provenance-bundle, and latest-result artifacts, and surfaced it in repaired/generated summary markdown plus regression coverage.
- Documented autonomous mode and hard stops in workflows/boundaries, enforced required doc fragments, added autonomous human-review stop smoke coverage, and fixed pre-existing validation drift so npm run validate passes.
