# Progress

- Ralph workspace initialized.
- Refined `.ralph/prd.md` into a concrete objective for a durable, file-backed multi-agent delivery framework spanning requirements, implementation, testing, and orchestration.
- Replaced the seed backlog in `.ralph/tasks.json` with repo-specific work derived from the actual `ralph-codex-vscode-starter` extension surface and current coverage gaps.
- Completed T3 by validating nested project-root selection for `ralph-codex-vscode-starter`, adding scanner/status regressions, and rebuilding the extension runtime artifacts.
- Completed T4 by adding regression coverage for nested workspace root-selection candidates and prompt repo-context rendering so nested child repos remain visible in generated prompts.
- Completed T5 by adding command-shell smoke coverage for prompt clipboard auto-copy and Open Codex IDE handoff modes, and by documenting the exact clipboard, IDE-command, fallback, and live-host testing boundaries.
- Completed T6 by documenting the manual `.vsix` install workflow, making `npm run package` succeed with `vsce package --no-dependencies` plus repository-aware README links, and validating a fresh `ralph-codex-workbench-0.1.0.vsix` build while capturing the remaining packaging warnings.
- Completed T7 by adding an optional real `codex exec` temp-workspace smoke command, documenting its environment-sensitive usage, and surfacing summarized execution failure messages into the structured Ralph iteration/latest-result/status artifacts after a sandboxed real-run preserved backend-connect failure evidence.
- Advanced T8 by persisting explicit workspace/inspection/execution/verification root policy in prompt evidence, execution plans, CLI invocation artifacts, iteration results, provenance bundles, and status summaries; `codex exec` plus validation/git verifiers now run from the selected inspection root while `.ralph` remains anchored at the workspace root, with nested-root regressions and focused docs updated. Manual override for ambiguous umbrella workspaces is still pending under T8.4.
- Completed T8 by adding the `ralphCodex.inspectionRootOverride` escape hatch for ambiguous umbrella workspaces, validating override paths against the workspace boundary, surfacing applied or invalid override state in scan evidence and status output, expanding override-focused regression coverage, and updating the nested-root docs to reflect the now-complete root policy.
- Completed T9 by locking the shipped automation scope to a sequential single-agent loop, documenting that broader multi-agent orchestration remains deferred until nested root semantics are deterministic and evidence-backed, and adding docs-validation coverage so the guardrail is test-backed.
- Use this file for durable progress notes between fresh Codex runs.
- Fixed the verifier normalization bug that left legacy `cd ralph-codex-vscode-starter && npm run validate` hints unnormalized when the repo itself was the opened workspace. Full `npm run validate` now passes again, but the retention task itself is not yet complete.
- Surfaced generated artifact retention in Show Status, added explicit enabled/disabled coverage, and aligned testing docs with the existing prompt/run/iteration cleanup behavior.
- Made generated-artifact cleanup honor an explicit allowlist of protected state/latest references and added regression coverage so unrelated path-like fields do not preserve old prompts, run artifacts, or iteration directories.
- Made generated-artifact retention precedence explicit as newest-N plus protected references, added regression coverage proving older protected entries remain retained in newest-first order, and aligned the retention docs with that rule.
- Added iteration-engine regression coverage proving blocked-preflight cleanup preserves the prior state-referenced prompt, transcript/last-message pair, and iteration artifact directory when generated-artifact retention is 1.
- Protected generated-artifact cleanup against deleting iteration files that are still referenced by latest-provenance-bundle.json or latest-provenance-failure.json, and added regression coverage for that retention case.
- Aligned the durable retention docs with the implemented behavior: generated-artifact cleanup now explicitly documents protection for paths still referenced by latest provenance bundle/failure pointers, and the testing doc now records that regression coverage.
- Protected generated prompts, run transcripts/last-message files, and iteration directories that are referenced only through durable state runHistory and iterationHistory, with regression and integration coverage.
- Aligned the retention invariants, workflow docs, and testing docs with the implemented generated-artifact protection behavior so `.ralph/state.json` history references (`runHistory[]` and `iterationHistory[]`) are explicitly treated as protected roots alongside `lastRun` and `lastIteration`.
- Extended generated-artifact protection so latest-prompt-evidence.json now protects the implied prompt file and iteration directory, added a regression proving it does not protect unrelated run artifacts, and updated retention docs to enumerate that protected-root rule.
- Added integration coverage proving generated-artifact cleanup preserves an older blocked integrity iteration when latest-provenance-failure.json is the remaining protected reference and retention is 1.
- Extended generated-artifact cleanup so run-only workspace state still protects the corresponding iteration directory, prompt, and transcript/last-message pair, then added regression coverage and doc updates for that fallback behavior.
- Protected prompt/transcript/last-message artifacts from raw state run references even when iteration is missing, added regression coverage for that boundary, and aligned invariants/testing docs.
- Added regression coverage and invariant/testing docs proving generated-artifact cleanup preserves older prompt/run/iteration artifacts when they are referenced only by latest-execution-plan.json or latest-cli-invocation.json. Validation passed with npm test.
- Extended generated-artifact retention so latest-result.json now protects referenced prompt and transcript/last-message artifacts, added regression and integration coverage, and aligned retention docs.
- Closed a remaining generated-artifact retention gap by deriving protected iteration references from lastRun when iterationHistory is absent, and added regression coverage for the lastRun-only state shape.
- Added explicit regression and doc coverage proving latest-preflight-report.json protects only the referenced iteration directory during generated-artifact cleanup; npm test passed.
- Made generated-artifact retention precedence explicit in the high-level README and architecture docs: Ralph keeps the newest parsed-iteration window first, then adds older protected references without evicting newer retained entries.
- Added focused artifact-retention regressions for latest-linked prompt/summary/preflight references and state-referenced transcript, last-message, and iteration-directory roots; updated testing docs accordingly.
- Added deterministic repair for deleted latest summary/provenance summary surfaces from surviving latest JSON metadata, wired status/open-latest flows through that repair path, and covered manual-deletion regressions.
- Expanded Show Status with recent iteration/run history, live generated-artifact and provenance-bundle retention summaries, and latest-artifact repair/stale reporting; added rendering tests and aligned workflow/testing docs.
- Added direct commands to open the latest prompt-evidence artifact and the latest CLI transcript with last-message fallback, plus registration/docs/smoke coverage.
- Added preflight artifact-readiness diagnostics for stale latest surfaces, missing latest-pointer targets, unsafe retention-root overlap, and disabled retention settings; wired them into iteration-start and status preflight paths; added regression coverage and doc updates.
- Added a scoped Cleanup Runtime Artifacts command that preserves durable Ralph state and latest evidence while pruning older generated prompts, run artifacts, iteration directories, provenance bundles, and logs; added regression coverage and updated docs.
- Hardened latest-artifact command recovery by making Open Latest CLI Transcript fall back to the persisted last-message artifact when latest CLI metadata points at a deleted transcript file, and added command-shell regression coverage. Validation passed with npm run validate.
- Fixed stale latest-cli-invocation fallback so Open Latest CLI Transcript now falls through to existing state-backed transcript or last-message artifacts when the latest invocation paths were manually deleted, and added command-shell regression coverage. Validation passed with npm run validate.
- Aligned AGENTS.md with the new latest-artifact and cleanup command surface added for T18; npm run validate passes cleanly on the current worktree.
- Removed the remaining vsce packaging warnings by adding a bundled MIT LICENSE file and a package.json files allowlist, updated release-build docs to describe the curated VSIX payload, and revalidated with npm run package plus npm run check:docs.
- Added a bounded, deterministic task-remediation recommendation for repeated no-progress and repeated identical failure outcomes, surfaced it in iteration/state artifacts and status/prompt context, and covered it with tests and docs.
- Implemented bounded structured child-task proposals for repeated no-progress remediation artifacts, surfaced proposal details in status output, updated docs, and validated with npm run validate.
- Added an explicit operator-approved command that applies the latest accepted decomposition proposal into .ralph/tasks.json while keeping remediation defaulted to propose-only.
- Added deterministic prompt-budget policy, task-aware prompt context trimming, and prompt-budget evidence persistence for CLI and IDE handoff flows; added regression coverage and verifier docs.
- Advanced T22 by implementing the T22.5 slice: added a default-medium `ralphCodex.reasoningEffort` setting, threaded it into `codex exec` as `model_reasoning_effort`, persisted the chosen value in transcript/invocation/integrity artifacts, and added regression coverage plus docs.
- Made prior-iteration prompt context task-aware so fix-failure and continue-progress prompts keep relevant remediation and validation signals under budget while omitting unrelated prior noise. Added regression coverage and passed full validation.
- Documented the prompt-budget policy, omission order, promptBudget evidence fields, and quota-saving reasoning-effort guidance in the focused workflow/provenance docs while preserving the existing validated implementation.
- Advanced T22 by tightening task-aware repo-context selection so prompt/CLI-focused tasks keep source-root context, and added regression coverage to keep budgeted prompt rendering aligned with code-facing prompt work.
- Advanced T22 by surfacing latest prompt-budget and reasoning-effort evidence in Show Status, with regression coverage and workflow docs so quota-sensitive CLI and IDE handoff behavior is inspectable from the primary status surface.
- Added explicit prompt-budget outcome accounting so prompt evidence and Show Status now report whether the final prompt stayed within target and by how many estimated tokens it was under or over; updated regression coverage and docs, and `npm run validate` passed.
- Allowed oversized continue-progress/iteration CLI prompts to omit progressContext after runtime and repo trimming, and added regression coverage proving the CLI budget path can stay within target by dropping recent progress when needed.
- Added regression coverage for token-budgeted human-review CLI prompts so blocker/remediation context is preserved ahead of progress trimming, and aligned workflow docs with that omission priority.
- Tightened prompt repo-context selection so generic cliExec prompts no longer include package/test inventory by default, while code-focused tasks still keep source and validation context; added regression coverage for code-focused and docs-focused CLI prompts and updated workflow docs.
- Extended prompt-budget evidence and status reporting to expose each prompt policy's required sections and fixed omission order, with docs and regression coverage; validation passed via npm run validate.
- Extended Show Status to display prompt-budget optional sections alongside required, omission-order, selected, and omitted sections; added regression coverage and aligned workflow documentation. Validation passed with npm run validate.
- Documented that bounded decomposition should start by reproducing the blocker against the inherited validation command, and added docs-validator coverage so the deterministic verification-target rule stays enforced.
- Resynced `.ralph/tasks.json` to the current worktree after repeated no-progress retries showed ledger drift: the stuck `T21.6.1.2` remediation-doc slice was already documented, the implemented `T22` prompt-budget work was still marked incomplete, and the backlog was corrected so Ralph will stop selecting stale tasks.
- Replenished the exhausted durable backlog with a new T23 slice focused on task-ledger integrity: detect done parents with unfinished descendants, surface tracker-drift warnings in preflight/status/backlog-replenishment context, add regression coverage, and document the operator repair path so future backlog exhaustion is less likely to come from bookkeeping drift.
- Surfaced task-ledger drift explicitly in preflight summaries, status output, and replenish-backlog prompt selection/strategy so inconsistent ledgers no longer read as clean backlog exhaustion. Added regression coverage for the new wording and prompt decision path.
- Re-inspected durable repo state and confirmed T23.1 is already implemented: done parents with unfinished descendants are reported as task-ledger drift in task-file inspection, surfaced through preflight/status consumers, and covered by regression tests. No code edits were necessary.
- Added regression coverage for done-parent ledger-drift diagnostics, exhausted-backlog loop stopping, drift-specific preflight blocking, and status rendering.
- Documented task-ledger integrity rules, explicit drift warnings, and the operator repair-vs-replenish decision path in the invariant and workflow docs. Validation passed with npm run check:docs.
- Auto-completed satisfied aggregate parent tasks during completion-report reconciliation so child-complete ledger drift no longer leaves stale backlog items behind.
- Added RalphTaskClaimStatus, RalphTaskClaim, and RalphTaskClaimFile to src/ralph/types.ts for a flat, inspectable task-claim schema consistent with tasks.json.
- Added optional agentId to RalphIterationResult and RalphRunRecord, defaulted legacy/missing records to the 'default' sentinel in state normalization, and persisted the sentinel on newly created single-agent iteration/run records. Compile passed.
- Updated .ralph/.gitignore to exclude /state.json and /done-task-audit*.md. Could not remove tracked runtime files from the Git index because the sandbox user is denied write access to .git, so .ralph/state.json, .ralph/done-task-audit-current-worktree.md, and .ralph/logs/extension.log remain tracked.
- Created ralph-codex-vscode-starter/docs/multi-agent-readiness.md with the required Task Ownership, Write Serialisation, Remediation Isolation, and Lifting The Deferral sections. The selected validation command still fails due to pre-existing absolute /home/admin/... links in the repository's existing docs, which the docs validator treats as outside-repo targets on this workspace.
- Extracted completion-report parsing from iterationEngine.ts into src/ralph/completionReportParser.ts and updated iterationEngine imports. TypeScript test compilation passed.
- Removed the remaining tracked Ralph runtime artifacts from the Git index now that .git was writable in this run. Validation confirms only prd.md, tasks.json, progress.md, and .gitignore remain tracked under .ralph.
- Added Ralphdex: Initialize Workspace to scaffold a fresh .ralph directory, documented the flow, and added smoke coverage for create and abort behavior.
- Documented the committed project-artifact vs operator-local runtime-state boundary in docs/boundaries.md and the root README, and updated docsValidator to require the new wording.
- Promoted docs/multi-agent-readiness.md into the durable documentation map, linked it from the single-agent boundary, and confirmed the docs validator passes.
- Added focused unit coverage for the extracted completion-report parser and cleaned the iteration engine import to depend only on the parser entrypoint.
- Added completion-report divergence fields to RalphProvenanceBundle, persisted them from executed iteration results including completion-report.json when present, added Model Self-Report summary output, and covered the rejected-report reconciliation case with regression tests.
- Added ralphCodex.autoReplenishBacklog config plumbing and updated loop stop logic to continue into backlog replenishment only when no actionable task remains, backlog.actionableTaskAvailable is false, and preflight shows no ledger-drift safety errors. Added loop logic coverage for the enabled path and the ledger-drift gate.
- Updated the Control-Plane Boundary wording to describe the shipped state as a sequential single-agent iteration/loop runner and kept the docs validator fixture aligned with that wording.
- Updated the Control-Plane Boundary sentence to describe the current shipped state while retaining the required 'single-agent iteration/loop runner' fragment, and docs validation passed.
- Updated docs/boundaries.md so the Control-Plane Boundary states the shipped control plane is a sequential single-agent iteration/loop runner and points to docs/multi-agent-readiness.md for the explicit acceptance criteria for future multi-agent orchestration. Documentation validation passed.
- Added docs/multi-agent-readiness.md to required docs, AGENTS/README doc-map targets, and DOC_RULES with the required headings and fragments. Ran npm run check:docs successfully.
- Confirmed completion-report parsing is already extracted into src/ralph/completionReportParser.ts and repaired the stale docs-validator fixture so the full test suite passes again.
- Extracted task decomposition and remediation helper functions from iterationEngine.ts into src/ralph/taskDecomposition.ts, updated iterationEngine imports, and verified with npm test.
- Extracted completion-report reconciliation from RalphIterationEngine into src/ralph/reconciliation.ts with explicit inputs and updated the engine to call the standalone helper.
- Verified that the code-owner entries, docs-validator updates, and architecture follow-on note are already present. `npm run validate` passes, but the task validation still fails because `src/ralph/iterationEngine.ts` is 1450 lines, so the remaining work is another extraction focused on the provenance-bundle and prepared-context assembly path.
- Extracted prompt/preflight/prepared-context assembly from iterationEngine.ts into iterationPreparation.ts, updated shared PreparedIterationContext imports, and reduced iterationEngine.ts to 1073 lines while keeping full validation green.
- Aligned repeated-failure remediation counting with the shared agent-scoped trailing-history helper in loopLogic.ts so interleaved history from another agent cannot accumulate into the same-task streak logic.
- Added regression coverage for per-agent repeated-no-progress isolation, preserved default single-agent behavior coverage, and documented agentId-scoped repeated-stop detection in verifier docs.
- Added dedicated regression coverage proving explicit agent-scoped remediation and stop detection do not inherit default-agent history, and verified with npm test.
- Hardened claim acquisition so an already-contested ledger returns contested even when the canonical latest claim matches the caller, and added regression coverage. Validation passed with `cd ralph-codex-vscode-starter && npm test`.
- Added regression coverage proving releaseClaim uses file-level locking under concurrent release attempts, with one released outcome, one not_held outcome, and a single released ledger record. Validation passed with npm test (227 passed).
- Added regression coverage proving concurrent releaseClaim calls serialize through the file lock and documented the expanded claim-lock coverage. Validation passed with `cd ralph-codex-vscode-starter && npm test`.
- Gated task selection and completion-report reconciliation on durable claim ownership, added claim_contested stop handling, and released claims after each iteration with regression coverage.
- Added a file-backed tasks.json write lock helper with retry/backoff timeout semantics, wired current task-file mutation paths through it, and added concurrency and timeout regression coverage.
- Added claim-graph inspection plus preflight and Show Status surfacing for contested, stale, and provenance-mismatched task claims; updated docs and regression coverage.
- Removed the validation blocker by adding a test-only process runner harness and fake git baseline so claim-lifecycle and handoff-path coverage now validates cleanly under sandboxed execution. Full `npm run validate` passes.
- Added an explicit operator-approved stale-claim recovery command that atomically marks canonical stale claims in claims.json, records durable resolution metadata, and surfaces the resolved task/provenance/reason in status and preflight reporting. Added regression coverage and updated operator docs.
- Added regression coverage for stale-claim recovery returning a task to CLI reselection and explicit status/doc wording for the claim lifecycle across CLI execution and IDE prompt preparation.
- Wrapped initializeFreshWorkspace tasks.json write in withTaskFileLock; all other mutation paths (reconciliation, stateManager.inspectTaskFile, stateManager.updateTaskFile) were already locked.
- Added lock-cleanup-on-throw regression test to taskFile.test.ts and documented the write-serialisation contract (lock path, hold duration, timeout, process-exit behaviour) in docs/invariants.md under a new Task Graph Write Serialisation section. All 268 tests pass.
- Serialised all tasks.json mutation paths (reconciliation, workspace seed) behind withTaskFileLock; added cleanup and concurrency regression tests; all 268 tests pass
- Lifted multi-agent deferral: updated boundaries.md, multi-agent-readiness.md, and invariants.md to record milestone satisfied 2026-03-17; relaxed single-agent fragment check in docsValidator; all 271 tests pass.
- Added Epistemic Gap section to docs/provenance.md covering the five required points: CLI provenance boundary, completionReportStatus machine-readable label, reconciliationWarnings semantics, absence-of-warnings meaning, and operator guidance to treat verifier artifacts as authoritative.
- markTaskInProgress atomically promotes todo→in_progress at claim time; beforeCoreState re-captured after selectClaimedTask so the bookkeeping change is not counted as agent progress; 285 tests pass, validate exits 0.
- Completed the remaining T36 stale-state preflight slice by making checkStaleState claim-specific. Concurrent agents no longer mask stale-claim warnings with unrelated iteration results or state timestamps; added regression coverage and aligned invariants.
- Added dedicated Agent Health rendering to Show Status and covered it with status-report tests. Full validate ran; new coverage passed, but an existing unrelated concurrent runCliIteration state.lock timeout still fails the suite.
- Verified durable repo state already satisfies T28.2: package.json exposes check:ledger and validate runs check:docs -> check:ledger before tests; docs/testing.md documents check:ledger under Authoritative Commands; docsValidator requires the check:ledger fragment for docs/testing.md. Ran npm run check:ledger and npm run check:docs successfully.
- No code change was necessary because docsValidator already enforces the Epistemic Gap heading and required provenance fragments for docs/provenance.md. Ran npm run validate: documentation validation passed, but the overall suite failed on unrelated existing checkLedger exit-code tests and one iterationEngine EPERM temp-lock test.
- Completed prompt calibration documentation and clarified that the documented baseline is tied to the checked-in runtime defaults in src/config/defaults.ts; docs validation passes.
- Existing worktree changes already add the required check-ledger regression coverage in test/checkLedger.test.ts using child_process.spawnSync against scripts/check-ledger.js. No further task-specific edit was needed.
- Surfaced the epistemic gap directly in executed provenance bundles via explicit model-claim vs verifier-evidence fields and summary sections, updated provenance/invariant docs, and tightened docs-validation and artifact-store regression coverage.
- Added regression coverage for auto-replenishment safety gates in loopLogic tests and documented backlog auto-replenishment safety and drift gating in docs/workflows.md. Task-specific loopLogic test passes; full validate still fails on the existing unrelated iterationEngine EPERM temp-lock test.
- Confirmed the promptBudgetProfile setting, customPromptBudget wiring, Claude profile selection, and regression coverage were already present; added the remaining workflow-doc clarification that the published matrix is the calibrated codex baseline and reran validation successfully.
- Fixed auto-replenish loop continuation to use clean backlog exhaustion instead of an unset stopReason, and added regressions for clean exhaustion vs blocked/drift cases.
- Inspected the durable state and confirmed T37.1 was already implemented: ralphCodex.agentId is configured and threaded through claim acquisition, run/iteration records, preflight default-agent collision warnings, and durable .ralph/agents/<agentId>.json history. Validation output showed compile/check-docs/check-ledger/lint/tests succeeded with 289 passing tests and 0 failures.
- Added scripts/calibrate-prompt-budget.js and documented the manual prompt-budget recalibration workflow in prompt-calibration.md and testing.md.
- Added a standard npm entry point for prompt-budget calibration and aligned README/testing/calibration docs around the model-configurable budget workflow. Full validation is currently blocked by a pre-existing TypeScript compile error in src/ralph/artifactStore.ts requiring activeClaimSummary on a persisted preflight report.
- Grouped active claims by agentId in preflight and Show Status, persisted the summary into preflight artifacts, added regression coverage for two-agent rendering and stale state, and documented multi-agent agentId/status usage.
- Added ralphCodex.autoReloadOnControlPlaneChange as a boolean setting defaulting to false in package.json, src/config/types.ts, src/config/defaults.ts, and src/config/readConfig.ts so the new control-plane reload toggle is available through the typed config surface.
- Added ralphCodex.autoApplyRemediation as an opt-in array setting with only decompose_task and mark_blocked allowed, defaulted it to empty array, and threaded it through package.json, config types, defaults, and config loading. Also aligned the existing autoReloadOnControlPlaneChange field in typed/default config so compile stays consistent.
- Added deterministic prompt scenario fixtures and a guard test covering bootstrap, partial-progress, repeated-no-progress remediation, blocked-task, and validation-failure cases.
- Wired loop-only auto-reload for control_plane_reload_required in registerCommands.ts. When autoReloadOnControlPlaneChange is true, Ralph logs the reload message, waits 1500ms for artifact flush, and executes workbench.action.reloadWindow; otherwise the existing operator-facing stop message remains unchanged.
- Added ralphCodex.autonomyMode as a supervised/autonomous shorthand, applied the effective override in readConfig, logged the resolved autonomy configuration at activation, updated the workflow doc, and added focused config plus activation tests.
- Added readable golden snapshot coverage for each prompt kind, expanded fixture scenarios to include replenish-backlog and human-review-handoff, generated plain .md snapshots, and documented npm test -- --updateSnapshot for snapshot refreshes. Validation remains red only because of unrelated pre-existing test failures in stateManager.test and iterationEngine.integration.test.
- Implemented automatic mark_blocked remediation application in the iteration engine using withTaskFileLock, persisted the required warning text in iteration results, logged the auto-apply event, and added an integration test covering the repeated blocked stop path.
- Persisted agentId into durable preflight, execution-plan, CLI-invocation, provenance-bundle, and latest-result artifacts, and surfaced it in repaired/generated summary markdown plus regression coverage.
- Documented autonomous mode and hard stops in workflows/boundaries, enforced required doc fragments, added autonomous human-review stop smoke coverage, and fixed pre-existing validation drift so npm run validate passes.
- Added the watchdog agent template and introduced RalphAgentRole with the watchdog role. The template reads preflight stale-state signals, inspects recent iteration history, emits structured watchdog_actions with severity, and explicitly forbids code-change proposals.
- Made autonomous mode explicitly preserve blocked-preflight hard stops in docs/config metadata and added regression coverage proving the CLI loop still halts on blocked preflight in autonomous mode.
- Added RalphAgentRole and ralphCodex.agentRole, introduced a review-agent prompt template with review-only contracts, and widened completion-report parsing for suggestedChildTasks.
- Implemented decompose_task auto-application through the shared proposal write path under withTaskFileLock in both the iteration engine and the apply-proposal command. Focused integration coverage for the remediation path passed.
- Added atomic clean-stop handoff notes under .ralph/handoff, wired handoff retention pruning into generated-artifact cleanup, and covered the behavior with targeted tests.
- Registered a single-pass Run Review Agent command, added per-run agent role/id overrides for review execution, and surfaced suggestedChildTasks in the output channel with an Apply Latest Task Decomposition Proposal prompt.
- Added brittle prompt content assertions for validation commands, blocker text, PRD objective text, remediation summaries, and bootstrap prior-context isolation; refreshed adjacent test fixtures and expectations so the full validate gate passes.
- Added fixture-driven semantic assertions to the prompt snapshot suite so each scenario now checks critical rendered guidance as well as exact markdown snapshots.
- Persisted commit-on-done SCM outcomes into iteration artifacts by moving the auto-commit step ahead of iteration result writes, and added regression coverage proving the warning is stored in iteration-result.json. Validation was run; the new SCM path passed, but the repo still has unrelated failing prompt-builder and state-manager tests.
- Added regression coverage for Run Review Agent command registration/single-pass behavior, verified review prompt routing to review-agent.md without implementation sections, and documented the review-agent workflow and approval boundary in docs/workflows.md.
- Added explicit command-shell regression assertions for control-plane auto-reload mode/command behavior and tightened docs/workflows.md wording for the reload command. Validation is currently blocked by unrelated TypeScript errors in src/ralph/iterationPreparation.ts.
- Wired watchdog-only reconciliation for resolve_stale_claim, decompose_task, and escalate_to_human; added parser and integration coverage, but repository compile is still blocked by unrelated iterationPreparation.ts errors.
- Implemented the missing review-agent anomaly handling so review-only runs that modify relevant workspace/source files now fail git-diff verification, surface explicit anomaly warnings, and no longer count those edits as progress. Added integration coverage and documented the workflow behavior.
- Implemented branch-per-task SCM orchestration with durable branch metadata, child-to-integration merges, atomic parent merges, conflict recovery, docs updates, and regression coverage.
- Re-inspected the durable repository state and confirmed T39.2 is already implemented: watchdog-only reconciliation permits cross-task resolve_stale_claim, decompose_task, and escalate_to_human, with parser and integration coverage. The prior compile blocker is no longer present; compile and watchdog-focused tests now pass.
- Registered the Run Watchdog Agent command, added smoke coverage for the watchdog command override, and documented watchdog usage, autonomous recovery actions, and escalation flow in workflows.md. Full validate still reports a pre-existing unrelated auto-reload smoke failure.
- Built on the existing scmPrOnParentDone implementation already present in durable state, then fixed the remaining validation blockers: concurrent inspectTaskFile seeding now tolerates losing the lock when another caller already completed the seed, and the loop auto-reload smoke test was stabilized. Full validate now passes.
- Fixed the two failing package-manifest tests by adding runWatchdogAgent and runScmAgent to package.json activationEvents and contributes.commands, and registering the missing runScmAgent command handler in registerCommands.ts. All 344 tests pass.
- Fixed Windows git commit failure caused by cmd.exe interpreting pipe characters in commit message body: changed processRunner.ts default from shell=true-on-win32 to shell=false.
- All child tasks done (T40.1/T40.2/T40.3); Windows git commit failure fixed via processRunner shell=false default; 344 tests pass; docs check passes.
- Fixed ledger inconsistency (T39 todo→in_progress), added runWatchdogAgent and runScmAgent to package.json activationEvents and contributes.commands, registered runScmAgent command stub, created missing test/packageManifest.test.ts source file. All 342 tests pass.
- Regression coverage and docs/workflows.md auto-reload section are in place; prior TypeScript blocker no longer exists; 347 tests pass.
- Ledger-integrity gate fully operational: check-ledger.js validates tasks.json + claims.json drift, wired into npm run validate, regression tests cover all drift classes — exits 0 on current workspace
- Added 3 regression tests for auto-remediation exclusion cases and documented autoApplyRemediation setting in docs/verifier.md and docs/workflows.md
- Backlog exhausted after T49 (executionIntegrity.ts extraction) stopped with control_plane_reload_required. Replenished with T55–T57: unit tests for executionIntegrity.ts (no test file existed), further iterationEngine.ts decomposition to ≤1100 lines (still 1,178 post-T49), and PRD advancement to reflect the lifted multi-agent deferral milestone.
- 11 executionIntegrity unit tests in place; fixed broken lint script (eslint not installed/configured — removed dead call); 411/411 tests pass, validate exits 0
- Extracted formatClaudeStreamLine+ClaudeStreamEvent to cliOutputFormatter.ts, autoApplyMarkBlockedRemediation+autoApplyDecomposeTaskRemediation to taskDecomposition.ts, and applyReviewAgentFileChangePolicy to reviewPolicy.ts; iterationEngine.ts now 1035 lines (target ≤1100); updated AGENTS.md and architecture.md
- Replaced stale 'Non-goal for now'/'Current implementation priority' sections with milestone record (satisfied 2026-03-17) and next delivery horizon (parallel multi-agent execution, health dashboard, end-to-end pipeline automation)
- Backlog exhausted after T57 (PRD advancement). Replenished with T58–T61: agentCount config + preflight/status exposure (T58), runMultiAgentLoop command with N concurrent claim-isolated loops (T59), Show Multi-Agent Status command from agents/ history files (T60), and watchdog durable diagnostic artifacts in .ralph/artifacts/watchdog/ (T61). These are the first concrete slice of the PRD's "Next delivery horizon".
- Added ralphCodex.agentCount config (number, default 1) to package.json, types, defaults, and readConfig; surfaced configured parallelism as an agentHealth info diagnostic in preflight and as a Loop line in Show Status; updated one preflight test and the statusReport snapshot fixture; 411/411 tests pass.
- Added ralphCodex.runMultiAgentLoop VS Code command that spawns N concurrent iteration loops with distinct agentIds (agentId-1..N) using the existing claim mechanism; package.json activation + command contribution, registerCommands.ts handler, packageManifest test, AGENTS.md + workflows.md docs updated; 412/412 tests pass
- Added ralphCodex.showMultiAgentStatus VS Code command that reads .ralph/agents/<agentId>.json files + latest handoff notes to render per-agent iteration history, current claim, and last-stop reason; package.json activation + command contribution, packageManifest test, AGENTS.md + workflows.md docs updated; 413/413 tests pass
- Watchdog diagnostic artifacts written to <artifactRootDir>/watchdog/<agentId>-<paddedIteration>.json on every processWatchdogActions invocation; retention cleanup added to cleanupGeneratedArtifacts and inspectGeneratedArtifactRetention; 2 new tests added; 415/415 pass
- Backlog exhausted after T61 (watchdog diagnostic artifacts). Replenished with T62–T66: stale-claim/repeated-no-progress heatmap in Show Multi-Agent Status (T62), runPipeline command skeleton with decompose→multi-agent-loop (T63), full E2E review-agent pass + PR submission wired into runPipeline (T64), durable pipeline provenance bundle linking PRD→tasks→iterations→PR URL (T65), and configurable human-review gate with approveHumanReview command (T66). These tasks complete the PRD health-dashboard pillar (T62) and deliver the entire end-to-end pipeline automation pillar (T63–T66).
- Confirmed T62.1 is complete: multiAgentStatus.ts implements computeStuckScore + buildMultiAgentStatusReport with WARNING/STUCK rendering; artifactCommands.ts wires IO (readAllHandoffsForAgent, readMultiAgentStatusSummaries); 17 regression tests pass; full validate gate: 462/462 pass
- Added buildNoProgressHeatmap (HEATMAP_WINDOW=10, X=no_progress/dot=other) to multiAgentStatus.ts; wired heatmap line into buildMultiAgentStatusReport; added 9 new tests covering heatmap rendering; 472/472 pass
- Added no-progress heatmap bullet to Show Multi-Agent Status section in docs/workflows.md; all implementation and tests already complete from T62.1 and T62.2
- Added ralphCodex.runPipeline command skeleton: scaffolds pipeline-root + child tasks from PRD headings, writes .ralph/artifacts/pipelines/<runId>.json, invokes multi-agent loop. 482/482 tests pass.
- Added Run Pipeline smoke test verifying review-agent → SCM-agent sequencing; fixed createMockRun to include execution field so pipeline command no longer throws before reaching SCM agent
- Added smoke test verifying pipeline artifact captures reviewTranscriptPath and prUrl from review→SCM phase; 507/507 tests pass
- Released stale T64.2 active claim that blocked check:ledger; added Configurable Human-Review Gate section to docs/workflows.md documenting pipelineHumanGates and approveHumanReview; all 509 tests pass
- Backlog exhausted after T66 (configurable human-review gate). Replenished with T67–T69: (T67) Open Latest Pipeline Run command + pipeline section in Show Status — two planned T65 deliverables that were not implemented before the control-plane reload; (T68) advance prd.md and AGENTS.md/architecture.md to record the three-pillar milestone satisfied 2026-04-02 and define the next horizon; (T69) pipeline crash-recovery — persist a phase checkpoint so a reloaded or crashed pipeline can resume from its last completed sub-phase via a new resumePipeline command.
- Added readLatestPipelineArtifact to pipeline.ts; wired Latest Pipeline section into buildStatusReport and collectStatusSnapshot; registered ralphCodex.openLatestPipelineRun in artifactCommands.ts and package.json; added pipeline, status-report, and package-manifest regression tests.
- Marked three-pillar delivery horizon satisfied (2026-04-02) in prd.md; defined next horizon (pipeline resume/crash-recovery, real E2E smoke test, operator CLI); added pipeline.ts + approveHumanReview/openLatestPipelineRun to AGENTS.md code owners and command list; added pipeline.ts to architecture.md module boundaries
- Pipeline crash recovery implemented: PipelinePhase type + phase checkpoint field added to PipelineRunArtifact; scaffoldPipelineRun writes phase:'scaffold'; runPipeline refactored to use shared runPipelineFromPhase helper with loop/review/done checkpoints; ralphCodex.resumePipeline command added with phase-aware re-entry and QuickPick selection; activation-time scan surfaces warning notification fo
- Backlog exhausted after T69 (pipeline crash recovery). All 183 prior tasks done. Replenished with T70–T73 covering the PRD's next delivery horizon: (T70) VS Code Marketplace readiness — publisher ID, keywords, PNG icon, CHANGELOG, release workflow docs; (T71) developer-loop shim to run the iteration engine without a VS Code host, enabling Ralph self-hosting; (T72) real end-to-end pipeline smoke test in a temp workspace; (T73) model tiering signal improvements and enable-by-default. T70 is highest priority per PRD. Next actionable task: T70.1.
- Set publisher to s0l0m0n8und9, added icon (128×128 PNG), added keywords array, created CHANGELOG.md with v0.1.0 entry, created docs/release-workflow.md. vsce package --no-dependencies passes with 0 blocking warnings; full validate passes (527 tests).
- All acceptance criteria verified: publisher=s0l0m0n8und9, 7 keywords, 128×128 PNG icon, CHANGELOG.md with v0.1.0 entry, release-workflow.md covering version-bump→publish→tag. validate passes (527 tests).
- All Marketplace metadata acceptance criteria satisfied: publisher=s0l0m0n8und9, 7 keywords, icon=media/ralph-icon.png (128x128 PNG). vsce package --no-dependencies clean.
- CHANGELOG.md added to package.json files allowlist; vsce package clean with changelog.md in VSIX
- Added Installation and Configuration sections to README.md; added docs/release-workflow.md to docs validator REQUIRED_DOCS and test fixture; fixed ledger drift (released stale T70.2 claim, marked T70.3 in_progress). 526/527 tests pass; 1 pre-existing failure in legacy task normalization.
- Defined IVSCodeHost (+ IOutputChannel, IProgress, IWorkspaceConfiguration, ICommandExecutor) in src/shim/types.ts; validate passes 527/527
- Resumed after the prior control-plane reload stop, released a stale T70.3 CLI claim that was breaking check:ledger, and stabilized processRunner stdout assertions for this sandbox. Full `npm run validate` now passes.
- Implemented the stdout-backed shim host and .ralph-config.json/env/default config reader, with unit coverage for file, env, and default resolution. Full validation now passes.
- Wired a standalone shim entry point that boots Ralph against a workspace path through stdoutHost plus .ralph-config.json/env config, excludes shim outputs from the VSIX payload, and added end-to-end/package regression coverage.
- Added an opt-in `test:e2e-pipeline` smoke script that seeds a temp workspace, runs `ralphCodex.runPipeline` through the harness with one real `codex` loop invocation, mocks review/SCM deterministically, asserts the pipeline artifact records a PR URL, documented local usage, and verified both the guarded skip path and the real opt-in path. `npm run validate` also passes.
- Replaced dependency/depth/blocked-status scoring with validation, child-task, and blocker-note signals in complexityScorer.ts, retained title word count as a capped ±1 signal, added focused unit coverage, and verified with npm run validate.
- Enabled model tiering by default, added ralphCodex.enableModelTiering flat toggle to package.json, wired it in readConfig, created docs/model-tiering.md with signal/threshold/override/cost-savings documentation, added docs validator rule requiring the new doc with required fragments including ralphCodex.complexityTierThresholds
- Backlog exhausted after T73.2 (model tiering enabled by default). Replenished with T74 (AI-driven project and PRD generation — a 5-subtask slice following the implementation plan at docs/superpowers/plans/2026-04-01-ai-project-generation.md) and T75 (rewrite PRD section 6 on skills/agents for clarity and deliverability). T74.1 is the first actionable task.
- All T74 subtasks already implemented by prior iterations. Cleaned up a split let/assign to const in projectGenerator.ts. npm run validate passes 534/534.
- All acceptance criteria confirmed satisfied: generateProjectDraft wired into initializeWorkspace and newProject with fallback, parseGenerationResponse tested, setProcessRunnerOverride coverage present, npm run validate passes 534/534.
- createCliProvider already exported; projectGenerator.ts imports and uses it correctly. All validation passes.
- PRD section 6 rewritten with concrete Phase 1 scope and deliverable slice (T76: extend PRD generation to include recommendedSkills array). Removed vague language, added scope boundaries, and created follow-on task.
- Fixed state deadlock: marked T74.2–T74.5 as done in tasks.json since all implementation was already present and validated. generateProjectDraft fully wired into initializeWorkspace and newProject with fallback; 541/541 tests pass.
- Prompt template updated to output {tasks, recommendedSkills} object; parseGenerationResponse extracts and validates recommendedSkills (tolerates missing/empty); generateProjectDraft return type extended; registerCommands persists recommended-skills.json in both initializeWorkspace and newProject; 13 new/updated tests added; 547/547 pass.
- Backlog exhausted after T76 (recommendedSkills in PRD generation). Replenished with T77–T79: (T77) prompt caching Phase 1 — stabilise promptBuilder.ts static prefix order and add byte-identical snapshot test; (T78) surface recommended skills in Show Status output (feature 6 Phase 2 — persistence done in T76, status surfacing not yet done); (T79) Azure Foundry provider Phase 1 — implement CliProvider interface in azureFoundryProvider.ts and register 'azure-foundry' as a valid CliProviderId. T77 is the first actionable task.
- Reordered all prompt templates to place static sections (Prompt Strategy, Operating Rules, Execution Contract, Final Response Contract) before dynamic sections. Added static/dynamic boundary comments in buildPrompt, exported STATIC_PREFIX_BOUNDARY and extractStaticPrefix. Added snapshot test asserting byte-identical static prefix across two task-differing builds. Regenerated all 7 existing snapsho
- Added recommendedSkills to RalphStatusSnapshot, buildStatusReport renders conditional Recommended Skills section, collectStatusSnapshot reads .ralph/recommended-skills.json, two unit tests added.
- Azure Foundry provider Phase 1 complete: AzureFoundryProvider implements CliProvider, 'azure-foundry' added to CliProviderId union in both cliProvider.ts and config/types.ts, createCliProviderForId wired in providerFactory, config fields added with defaults and readConfig support, 12 unit tests added, 565/565 pass.
- Backlog exhausted after T79 (Azure Foundry Phase 1). Replenished with T80–T84: (T80) Azure Foundry Phase 2 — add missing PRD-specified config settings (apiKey, modelDeployment, apiVersion) and package.json contributions; (T81) Azure Foundry Phase 3 — convert provider to direct HTTPS execution via Node.js https module; (T82) Azure Foundry Phase 4 — API key and Azure AD auth with preflight validation; (T83) Prompt caching Phase 2 — cache_control breakpoints for direct-API providers at the static/dynamic boundary; (T84) Recommended Skills Phase 3 — Construct Recommended Skills operator command. T80 is the first actionable task (no dependencies). T84 is also independently actionable.
- Fixed two pre-existing validation blockers: (1) check-ledger.js path resolution bug (going up 2 levels instead of 1); (2) promptBuilder template CRLF normalization causing static prefix boundary test failure on Windows. All 5 Azure Foundry settings and azure-foundry enum were already present in types/defaults/readConfig/package.json from the prior iteration.
- CliProvider.executeDirectly optional method added; AzureFoundryProvider implements it via httpsPost (node:https wrapper with test override); CliExecCodexStrategy prefers executeDirectly over child-process spawn; 4 unit tests for direct HTTPS path; strategy preference test added.
- API key sent as api-key header in HTTPS requests; Azure AD stub warning emitted when key absent; key value never written to transcripts/provenance (test verified); preflight emits error for missing endpoint, info for missing key (Azure AD stub), info for configured key; 6 new unit tests; npm run validate: 591 pass 0 fail
- Added cache_control breakpoints to AzureFoundryProvider.executeDirectly; added PromptCacheStats to RalphPromptEvidence and RalphProvenanceBundle; wired through iterationEngine and provenancePersistence; surfaced in Show Status; 5 new unit tests added.
- constructRecommendedSkills command fully implemented, registered, tested, and documented. All 600 tests pass.
- Backlog exhausted after T84 (Recommended Skills Phase 3). Replenished with T85–T90: (T85) Prompt caching Phase 4 — add ralphCodex.promptCaching config setting (auto/force/off); (T86) Azure Foundry Phase 5 — document prompt caching synergy and Azure Monitor cache-hit verification; (T87) Static task-complexity tier Phases 1+2 — add tier field to RalphTask and short-circuit selectModelForTask; (T88) Static tier Phase 3 — document tier in AGENTS.md; (T89) Static tier Phase 4 — surface effective tier in Show Status; (T90) Agent memory management Phases 1+2 — add memoryStrategy config and implement sliding-window in promptBuilder. T85 and T87 are independently actionable; T85 is the higher-priority first pick.
- Added ralphCodex.promptCaching (auto/force/off, default auto); off suppresses cache_control in AzureFoundryProvider.executeDirectly; force emits warning on CLI-based providers lacking direct-HTTPS caching; 4 new unit tests; docs/prompt-calibration.md updated
- Added 'Prompt Caching And Azure AI Foundry' section to docs/model-tiering.md covering supported model families, CLI-implicit vs direct-API-explicit caching distinction, and step-by-step Azure Monitor cache-hit verification.
- Added RalphTaskTier type and tier field to RalphTask/RalphSuggestedChildTask; selectModelForTask short-circuits on explicit tier with explicit signal; taskDecomposition propagates tier to children; 3 unit tests added; all 607 tests pass
- Added Task Schema section to AGENTS.md documenting optional tier field with example; added tier to invariants.md schema fragment; check:docs passes
- Surfaced effective tier in Show Status (current task + last task labels with explicit/scored distinction) and Show Multi-Agent Status (claim task with explicit/dynamic label); 10 regression tests added; 617 tests pass
- Phases 1+2 complete: memoryStrategy/memoryWindowSize/memorySummaryThreshold added to config; sliding-window support implemented in promptBuilder; 2 new tests added; 619 tests pass
- Backlog exhausted after T90 (agent memory management Phases 1+2). Replenished with T91–T97: (T91) agent memory Phase 3 — summary strategy in promptBuilder; (T92) agent memory Phase 4 — memoryStrategy observability in provenance bundle; (T93) operator mode presets Phases 1+2 — schema, package.json, preset resolution in readConfig; (T94) operator mode presets Phases 3+4 — Show Status surfacing and AGENTS.md documentation; (T95) PRD creation improvements Phases 1+2 — task count cap and operator-overridable template; (T96) PRD creation improvements Phases 3+4 — worked examples and suggestedValidationCommand; (T97) PRD creation improvements Phase 5 — Regenerate PRD command. T91 and T93 and T95 are independently actionable; T91 is the highest-priority first pick (continues the in-progress memory management feature).
- Summary memory strategy implemented in promptBuilder: buildSummaryStrategyContext reads memory-summary.md when above threshold, maybeSummariseHistory in iterationPreparation invokes CLI and persists summary; 2 new tests added; 621 tests pass
- memoryObservability (memoryStrategy, historyDepth, windowedEntryCount, summaryGenerationCost) added to RalphPromptEvidence and RalphProvenanceBundle; populated in promptBuilder and provenancePersistence; Show Status surfaces strategy, depth, and windowed entry count; 4 new tests cover all three strategies; 625 tests pass
- Added OperatorMode type + operatorMode field to config; OPERATOR_PRESETS map for simple/multi-agent/hardcore; preset resolution as fallback in readConfig; package.json enum contribution; 3 unit tests; 628 tests pass
- Surfaced operatorMode in Show Status with preset/explicit provenance; added 4 regression tests; AGENTS.md preset reference table; docs/workflows.md hardcore warning; 632 tests pass
- Phases 1+2 complete: task count cap (5-8) added to generation prompt with rationale; parseGenerationResponse sets taskCountWarning when >8 tasks returned; prdGenerationTemplate config key added across types/defaults/readConfig/package.json; generateProjectDraft uses template when set; 3 new tests; 635 tests pass
- Phases 3+4 complete: GENERATION_PROMPT_TEMPLATE now includes Good vs Bad task formulation section with Ralphdex-specific examples; suggestedValidationCommand added to JSON schema; parseGenerationResponse extracts it into task.validation; 2 new tests; 637 tests pass
- ralphCodex.regeneratePrd command registered: reads prd.md, generates refined PRD via generateProjectDraft, opens vscode.diff for operator review, writes on Apply; package.json activation event + command contribution added; packageManifest test added; AGENTS.md updated; docsValidator maxNonEmptyLines bumped 90→91
- Backlog exhausted after T97 (PRD creation improvements Phase 5: Regenerate PRD command). All PRD items 0-12 are complete (T70-T97). Replenished with T98-T102: (T98) Dynamic planning layer Phase 1 — agent role schema and crew.json definition; (T99) Dynamic planning layer Phases 2+3 — role-aware task claiming and role-specific prompt templates; (T100) Dynamic planning layer Phase 4 — pre-execution planning pass (inline and dedicated modes); (T101) Dynamic planning layer Phase 5 — crew configuration documentation and AGENTS.md update; (T102) Intelligent failure recovery Phase 1 — FailureCategoryId taxonomy and diagnostic pass artifact. T98 is the first actionable task (no dependencies). T102 is also independently actionable.
- Dynamic planning layer Phase 1 complete: RalphAgentRole expanded to include planner/implementer/reviewer; default changed to implementer; crew.json roster schema defined with parseCrewRoster(); multi-agent launcher uses crew roster when present, falls back to agentCount when absent, warns on invalid schema; 9 new tests added.
- Role-aware task claiming and role-specific prompt templates complete: selectNextTaskForRole, planning.md, review.md, parser updates, 10 new tests, 657 pass
- Fixed duplicate import in iterationPreparation.ts; added 14 unit+integration tests covering all T100 acceptance criteria (parsePlanningResponse, writeTaskPlan, Task Plan prompt section, suggestedValidationCommand reconciliation). 671 tests pass.
- Added Crew Configuration section to AGENTS.md (crew.json format, agentRole, planningPass config, 3-agent example); added Planning Pass section to docs/workflows.md (inline vs dedicated modes, 2-call cost, verification steps, default=disabled); bumped docsValidator maxNonEmptyLines 91→102. 671 tests pass.
- Backlog exhausted after T102 (Intelligent failure recovery Phase 1: FailureCategoryId taxonomy and diagnostic pass artifact). All PRD items 0-13 complete; item 14 Phase 1 complete. Replenished with T103-T107: (T103) Intelligent failure recovery Phase 2 — recovery orchestrator with playbook dispatch; (T104) Intelligent failure recovery Phase 3 — dead-letter queue and requeue command; (T105) Intelligent failure recovery Phase 4 — failure chain detection and systemic alert; (T106) Intelligent failure recovery Phase 5 — observability, configuration, and documentation; (T107) Webview UI Phase 1 — WebviewPanelManager, MessageBridge, activity bar, and shared styles. T103 is the first actionable task (continues failure recovery). T107 is independently actionable (starts PRD item 15).
- Recovery orchestrator with playbook dispatch implemented: dispatchRecovery selects playbook by rootCauseCategory, persists recovery-state.json, gates side effects behind autoApplyRemediation, 14 unit tests covering all AC criteria, 711 tests pass.
- Dead-letter queue (dead-letter.json), requeue command (ralphCodex.requeueDeadLetterTask), status surface integration, and 9 unit tests covering all AC criteria. 720 tests pass.
- Added failure-chain rolling window (per-run, keyed by artifactRootDir), systemic alert detection (3+ distinct tasks, confidence>=0.7), SystemicFailureAlert artifact emission, pause-all-agents signal, and 3 unit tests covering all AC criteria. 723 tests pass.
- Added acceptance-criteria coverage for diagnosticCost persistence in provenance bundles and current-task recovery status rendering in Show Status. Compile, docs, ledger, prompt-budget, and lint passed; the test phase was blocked by sandbox-level spawn EPERM before executing the suite.
- WebviewPanelManager, MessageBridge, shared stylesheet, showSidebar command, placeholder navigation views, 13 unit tests, AGENTS.md updated. npm run validate passes (740 tests, 0 failures).
- Backlog exhausted after T107 (Webview UI Phase 1). Replenished with T108-T112 for the next coherent Webview UI Phase 2 slice: consolidate dashboard state onto durable status snapshots, migrate dashboard hosting onto src/webview foundations with a showDashboard command, add the pipeline/agent/task/failure sections, normalize dashboard cost-ticker inputs, and route Show Status workflows through the dashboard. T108 is the next actionable task.
- Defined DashboardSnapshot type and buildDashboardSnapshot assembler in src/webview/dashboardSnapshot.ts; 9 unit tests pass; npm run validate clean at 749 tests.
- Added ralphCodex.showDashboard command; migrated dashboard lifecycle onto WebviewPanelManager and MessageBridge via shared DashboardHost; sidebar and panel share one controller; 8 new regression tests; 758 tests pass, npm run validate clean.
- Dashboard panel now loads durable status, pipeline, agent, dead-letter, and failure data and renders the Phase 2.3 sections with command-routed actions. Focused dashboard snapshot and panel tests were updated for empty and populated states.
- Aligned the dashboard quick-actions bar with the approved command surface by wiring latest run, provenance bundle, prompt evidence, CLI transcript, status, and VS Code settings actions through command-routed buttons; added focused regression coverage for those commands.
- Added an explicit Done metric to the dashboard task board so durable status mix now shows completed work alongside todo, in-progress, blocked, and dead-letter counts. Preserved the prior quick-actions command wiring and added regression coverage for the new task-board metric.
- Extended the dashboard panel to show each agent's durable first-seen timestamp and added regression coverage asserting both the pipeline-strip latest-run command link and the new agent metadata.
- Improved the dashboard failure feed so it now surfaces up to 5 recent failure events from the selected task and dead-letter diagnostic history, with regression coverage for ordering/capping and multi-entry rendering.
- All six dashboard sections implemented and tested: pipeline strip, task board, agent grid, failure feed, dead-letter, quick actions. Quick-actions bar wires through the approved command surface. 763 tests pass.
- Added dashboard cost-ticker: executionCostUsd persisted from claude CLI stream-json, DashboardCostSection typed with explicit null/unavailable semantics, cost ticker card rendered in panelHtml, 4 unit tests added, 767 tests pass
- Routed showRalphStatus and showMultiAgentStatus through ralphCodex.showDashboard + ralphCodex.refreshDashboard; raw reports still written to output channel; docs and tests updated; 768 tests pass.
- Tabbed dashboard layout implemented outside the Ralph task system (commits e48af39–e30c978): phase-based tab navigation, snapshot status handling, force-render for critical phase transitions, and dashboardHost test coverage. 772 tests pass.
- Backlog exhausted after T112 and subsequent manual dashboard tab work. Replenished with T113–T117: (T113) configuration/default consistency hardening — align planningPass.enabled and promptBudgetProfile defaults across package.json/defaults.ts/docs and add a check:docs drift gate; (T114) Azure Foundry authentication completion — implement DefaultAzureCredential keyless auth path and upgrade preflight to auth-readiness checks; (T115) provider-agnostic memory summarization — replace hardcoded claude -p - summarization with provider strategy abstraction; (T116) documentation and operator-trust reconciliation — remove placeholder text, add feature maturity markers, add PR config-change checklist; (T117) Webview UI Phase 3 PRD Creation Wizard skeleton. T113 and T114 are independently actionable; T113 is the highest-priority first pick.
- Aligned planningPass.enabled and promptBudgetProfile defaults between package.json and defaults.ts; added config-default drift check to check:docs; Show Status now reports effective value and source for both settings
- All acceptance criteria verified: DefaultAzureCredential bearer token path implemented in azureFoundryProvider.ts, API-key path preserved with redaction, preflight emits 4 auth-readiness diagnostics (endpoint_missing error, auth_azure_ad warning, no_auth_available error, auth_api_key_active info), 13+ unit tests cover both auth paths and all failure modes. npm run validate passes with 776 tests, 0
- All acceptance criteria verified as complete: (1) maybeSummariseHistory routes through provider.summarizeText — no hardcoded claude -p - remains; (2) summarizationMode field (provider_exec | fallback_summary) is explicit in memory-summary.md and surfaced in RalphMemoryObservability/provenance; (3) preflight emits memory_summarization_fallback info diagnostic when fallback is active; (4) memorySumm
- Fixed 3 manifest-description contradictions (planningPass.enabled 'default true'→'default false', memoryStrategy summary 'placeholder'→actual behavior, memorySummaryThreshold 'reserved for future use'→active description). Added maturity markers (stable/beta/experimental) to docs/workflows.md for planning pass, prompt budget profiles, memory strategy, and Azure AI Foundry provider. Added new 'Memor
- Added a PRD creation wizard webview with generate-preview-review-confirm flow, registered newProjectWizard in the manifest and command wiring, routed regeneratePrd into the wizard at step 4 with the current PRD preloaded, and added host/manifest/command tests. Full validation passed via npm run validate.
- Backlog exhausted after T117. Replenished with T118 and T118.1-T118.4 because the shipped wizard is still a Phase 3 skeleton: the PRD contract still calls for richer project-type/objective/constraints inputs, an editable generate step with regenerate comparison, full task-card editing, and config application at confirm time. T118.1 is the next actionable task.
- Expanded the PRD creation wizard intake to the broader project-type set, added project-type-aware objective guidance with a character counter, split constraints into structured fields while preserving the existing generateDraft contract, and added host plus webview coverage. Validation passed via npm run validate.
- Made step 4 editable with dedicated draft-text state, preserved regenerate comparison context as a stable current-PRD baseline, and added focused host tests covering comparison seeding plus manual draft edits surviving later steps and confirm-write.
- Upgraded the PRD wizard task review cards to support title edits, deterministic move up/down reordering, deletion, and guarded confirm-write validation. Hardened the locked tasks.json replacement path to preserve reviewed order under the flat v2 schema, and fixed task-file normalization to retain explicit tier overrides. Added regression coverage for wizard editing/safeguards, persisted write norm
- Implemented selectable operator-mode and CLI-provider recommendations in the PRD wizard, applied selected settings through the VS Code workspace configuration API at confirm time, and expanded the write summary plus regression coverage for selection state, persistence, and skipped recommendations.
- Fixed a Phase 3 PRD wizard persistence gap: confirm now always rewrites recommended-skills.json to match the operator's current selection, including empty selection, and added regression coverage for stale-file replacement. Full validation passed.
- Added explicit current-vs-draft comparison support to the regenerate PRD wizard flow. The host now emits a comparison summary in regenerate mode, the webview shows it in step 4, and regression coverage verifies the summary updates when generation or manual edits diverge from and return to the current PRD baseline.
- Completed the remaining PRD wizard Phase 3 contract gaps by adding regenerate-mode current-vs-draft comparison summaries in step 4 and ensuring confirm rewrites recommended-skills.json even when the operator deselects every skill. Focused wizard tests and full npm run validate both passed.
- Closed the remaining documentation gaps for the normalized-task contract: added test/taskContract.test.ts coverage entry to docs/testing.md and added normalization responsibility plus invariants.md cross-reference to docs/architecture.md. All acceptance criteria are now satisfied.
- Created src/ralph/taskNormalization.ts with normalizeNewTask — a single reusable pipeline that canonicalizes IDs, trims text, deduplicates arrays, maps aliases (rationale→notes, structured dependsOn, null validation), augments from parent, and delegates to normalizeTask. Wired into applySuggestedChildTasks and buildPipelineRootTask. Added 28 tests. Updated docs/invariants.md. Full validation passe
- Routed PRD generation, wizard persistence, and command-driven task creation through a shared normalization/persistence pipeline. AI-generated and bootstrap tasks now preserve richer supported fields instead of being narrowed before writing tasks.json.
- Added a shared producer-facing child-task persistence helper in src/ralph/taskCreation.ts, routed decomposition/remediation/pipeline child creation through it, and moved pipeline root append onto the same task-creation pipeline. Updated docs/tests to make the shared extension point explicit and verified with npm run validate.
- Added regression coverage for shared task persistence, PRD wizard persistence, remediation child-task persistence, Initialize Workspace, New Project, and Add Task producer paths; validated the repo with npm run validate.
- Documented the shared generated-task invariant across README, invariants, and workflow docs. Clarified that generated tasks should retain the richest known fields through the shared normalization pipeline, while sparse output is only canonical when the source genuinely lacks that data or the producer is intentionally minimal such as the pipeline-root scaffold.
- Re-inspected the repo and verified that the shared task-creation invariant is already implemented across taskNormalization/taskCreation, producer routing, docs, and regression coverage. No further safe code change was required; npm run validate passed fresh.
- Backlog replenishment was required because T119 closed the shared task-creation invariant slice and left no actionable backlog items. Re-inspected the PRD, current webview foundations, and shipped command surface, then added T120-T120.4 as the next coherent slice to finish PRD item 15's remaining settings panel, task view, and failure-detail UI work on top of the existing dashboard/wizard stack.
- Added a manifest-backed typed settings-surface contract, routed dashboard settings rendering through shared metadata, implemented NEW-setting discovery with one-time notification and deep-link focus, and added regression coverage. Validation passed with npm run validate.
- Added an inline settings-panel provider test action backed by a new internal command, extracted provider-readiness diagnostics from preflight into a shared helper, and added panel plus command regressions covering the new path.
- Implemented the Ralphdex task tree view over durable tasks, claims, dead-letter state, and per-task plan/diagnostic artifacts; added Show Tasks focus wiring and expanded watcher coverage for dead-letter and task artifact refresh.
- Gated failure-diagnosis notifications on fresh failure-analysis artifact writes to prevent stale recovery toasts, and added smoke coverage for View Diagnosis, Auto-Recover task-ambiguity routing, and Skip Task blocker persistence.
- Routed Open Failure Diagnosis through the shared dashboard diagnostics tab, updated smoke coverage to reject the legacy dedicated diagnosis panel path, aligned operator docs, and passed npm run validate.
- Backlog replenishment was required because T120 closed the remaining shipped webview UI contract work and left no actionable tasks. Re-inspected the PRD, README, testing docs, and current pipeline smoke harness, then added T121-T123 as the next coherent slice to finish the still-partially-mocked `test:e2e-pipeline` path into a hermetic full-phase pipeline smoke with aligned docs and regression coverage.
- Replaced the pipeline E2E smoke's runCliIteration monkey-patch with deterministic fake Codex executable hooks that drive build, review, and SCM through the normal provider path, added regression coverage for the fixture, and updated testing docs. Validation passed via the focused fixture test, the opt-in pipeline smoke, and npm run validate.
- No additional code changes were required in this iteration. Verified the existing T122 implementation in the current workspace by running the Windows-safe E2E smoke command and the targeted verifier regression suite; both passed, confirming the full pipeline phase chain and portable env-prefix validation handling.
- Aligned operator-facing testing docs with the full pipeline smoke contract, added a regression requiring the E2E smoke summary to expose the delegated phase-command chain, updated the smoke script to emit that evidence, and passed npm run validate.
- Backlog replenishment was required because T123 closed the full pipeline smoke slice and left no actionable tasks. Re-inspected the PRD, README, release docs, and current repo state, then added T124-T126 as the next coherent slice to finish the still-open Marketplace-readiness horizon: correct Marketplace-facing docs and stale repo-root commands, add an explicit `vsce publish --dry-run` validation path, and guard that release workflow against future drift.
- Closed the remaining Marketplace-readiness gaps by reviewing package metadata, adding Marketplace-grade install and post-install demo guidance to README, and correcting release-workflow commands to the current repo root with explicit Marketplace verification steps.
- Added a repo-root publish:dry-run script, made the Marketplace release workflow require dry-run validation before real publish, and extended docs validation/tests so the dry-run-first release contract stays enforced.
- Added forbiddenFragments to DocRule, added docs/release-workflow.md to DOC_RULES with required headings/fragments and forbidden stale path, updated seed, added regression test for forbidden_fragment guard.
- Backlog replenishment was required because T126 closed the Marketplace-readiness drift-guard slice and left no actionable tasks. All 15 items from the "next delivery horizon" defined after the three-pillar milestone (2026-04-02) are now shipped. Added T127–T129 as the next coherent slice: (T127) advance prd.md to record the 15-item horizon as satisfied and define a post-launch horizon; (T128) prepare the v0.2.0 release with a comprehensive CHANGELOG entry and version bump; (T129) model tiering calibration — run the complexity scorer over the 267 completed tasks and document findings. T127 is the first actionable task; T128 depends on T127; T129 is independently actionable.
- prd.md updated: 15-item horizon marked satisfied 2026-04-14 with compact summary; new next-horizon section added with three concrete items (v0.2.0 release prep, shim self-hosting validation, model tiering calibration); npm run check:docs passes
- Fixed vsce packaging: restored .vscodeignore blocklist, removed broken files array from package.json (negation entry !out/shim/** caused minimatch to re-include all ignored files), updated packageManifest test. npm run package produces clean 241-file VSIX; npm run validate passes 885/885.
- Created scripts/calibrate-tiering.js (scores all 269 done tasks), appended ## Calibration section to docs/model-tiering.md with tier counts, histogram, representative examples, and threshold assessment
- Backlog replenishment required after T129 (model tiering calibration) completed the last task in the post-launch horizon. All three post-launch horizon items are now done (T128 v0.2.0 release, T129 model tiering calibration, T127 prd.md advancement). The only remaining post-launch horizon item — developer-loop shim self-hosting validation — was never tracked as a task. Added T130–T132: (T130) validate the Node.js shim end-to-end against this repo's .ralph/ workspace and document findings in docs/shim-validation.md; (T131) advance prd.md to record the post-launch horizon as fully satisfied; (T132) begin the Continued multi-agent evolution horizon with Item 20 Phase 1 — define OrchestrationGraph/OrchestrationState types and scaffold orchestrationSupervisor.ts. T130 is the first actionable task.
- Added `T130.1` as a dedicated shim smoke-validation child task so the Node.js shim can claim and reconcile a disposable task without colliding with the parent validation task `T130`; updated `docs/shim-validation.md` to make that operator workflow explicit.
- Recorded post-launch horizon completion milestone and identified continued multi-agent evolution as active next target
- All acceptance criteria verified: types in types.ts, orchestrationSupervisor.ts with evidence-gated advanceState, artifact paths in artifactStore.ts, comprehensive unit tests (896 pass), AGENTS.md and architecture.md updated. npm run validate passes cleanly.
- Backlog replenishment required after T132 (Item 20 Phase 1) completed and left the backlog exhausted. T132 stopped with control_plane_reload_required/partial_progress but was later fully reconciled — all acceptance criteria satisfied and the task is marked done. Added T133–T135 as the next coherent slice continuing the Continued multi-agent evolution horizon: (T133) Item 20 Phase 2 — per-node span artifacts (OrchestrationNodeSpan type, writeNodeSpan/readNodeSpan helpers, node-<id>-span.json paths); (T134) Item 20 Phase 3 — pipeline integration (orchestration graph pointer in pipeline run artifacts) and Show Status orchestration display (current node, prior outcomes, pending branches); (T135) Item 21 Phase 1 — explicit handoff contract schema and durable lifecycle (RalphHandoff type, handoffManager.ts with propose/accept/reject/expire/contested lifecycle, .ralph/handoffs/<handoffId>.json persistence). T133 is the first actionable task; T134 depends on T133; T135 depends on T134.
- OrchestrationNodeSpan interface, nodeSpanPath helper, writeNodeSpan/readNodeSpan helpers, and 4 unit tests added. All acceptance criteria satisfied.
- All acceptance criteria confirmed satisfied: orchestrationGraphPath in pipeline artifact (pipeline.ts:219), orchestration snapshot collection in statusSnapshot.ts (lines 404-448), Orchestration section rendering in statusReport.ts (lines 526-543), three regression tests in statusReport.test.ts (lines 1346-1390). npm run validate passes 903/903 tests.
- RalphHandoff interface, handoffManager.ts lifecycle (propose/accept/reject/expire/getStatus/isExpired), 24 unit tests, AGENTS.md code-owner entry, architecture.md update, docs validator budget bump. All 920 tests pass.
- Backlog replenishment required after T135 (Item 21 Phase 1) completed and left no actionable tasks. All of Item 20 (phases 1-3, T132-T134) and Item 21 Phase 1 (T135) are done. Added T136-T139 as the next coherent slice: (T136) Item 21 Phase 2 — stale/contested handoff detection in preflight Agent Health; (T137) Item 21 Phase 3 — handoff reconciliation checks in reconciliation.ts; (T138) Item 21 Phase 4 — latest-handoff artifacts and Show Status handoff section; (T139) Item 22 Phase 1 — role policy map schema and context envelope types. T136 is the first actionable task; T137 depends on T136; T138 depends on T137; T139 depends on T138.
- checkHandoffHealth exported from preflight.ts: warns on expired proposed/accepted handoffs, errors on contested handoffs with operator/watchdog resolution instruction; wired into iterationPreparation and statusSnapshot; 4 unit tests added; npm run validate passes
- Added handoff scope check to reconcileCompletionReport: scanAcceptedHandoffs helper, needsHumanReview field on CompletionReportArtifact, warning emitted when accepted handoff taskId mismatches completion report selectedTaskId, 4 unit tests added
- Phase 4 complete: writeLatestHandoffArtifacts in handoffManager writes latest-handoff.json + summary on every lifecycle event; collectStatusSnapshot reads it; buildStatusReport renders Active Handoffs section guarded by null check; 6 new regression tests added.
- Added RolePolicy/RolePolicyMap/ContextEnvelope to types.ts, created rolePolicy.ts with DEFAULT_ROLE_POLICY_MAP (all 7 roles) and getEffectivePolicy, added contextEnvelopePath to artifactStore.ts, 10 new unit tests all pass
- Backlog replenishment: T139 (Item 22 Phase 1) completed and left no actionable tasks. Added T140–T143 as the next coherent slice continuing Item 22 and beginning Item 23: (T140) Item 22 Phase 2 — role-specific context selectors in promptBuilder.ts and context-envelope artifact persistence; (T141) Item 22 Phase 3 — policy enforcement in reconciliation/execution paths with policy_violation stop classification; (T142) Item 22 Phase 4 — role-policy diagnostics in preflight and Show Status; (T143) Item 23 Phase 1 — PlanGraph/ExecutionWave schema and fan-out wave safety rules. T140 is the first actionable task.
- Validation gate confirmed clean: 949/949 tests pass, all four acceptance-criteria tests present and green. No code changes required — implementation was complete at prior session end.
- writeContextEnvelope helper and contextEnvelopeWriter.ts fully implemented and tested; iterationPreparation.ts writes envelope after prompt construction; 949 tests pass
- All acceptance criteria confirmed: role-based section exclusions in promptBuilder.ts, writeContextEnvelope persists context-envelope.json artifact, four required unit tests present and green. npm run validate passes 949/949.
- Policy enforcement gate added to reconcileCompletionReport and iterationEngine; policy_violation added to RalphStopReason; docs/verifier.md updated; 6 regression tests added; all 955 tests pass.
- Role-policy diagnostics added to preflight (role_policy_effective info diagnostic) and Show Status (## Role Policy section); collectStatusSnapshot reads policySource from context-envelope artifact; 2 new regression tests added; all 957 tests pass
- Added PlanGraph/ExecutionWave types, writeRiskLabels field on RalphTask, planGraphPath helper, writePlanGraph/readPlanGraph/validateWaveSafety in planGraph.ts, and 11 unit tests covering all acceptance criteria scenarios. 968/968 tests pass.
- Backlog replenishment: all 287 prior tasks exhausted (all done). Added T144–T147 as the next coherent slice completing Item 23 (Adaptive re-planning + fan-out/fan-in reconciliation): (T144) Phase 2 — fan-in gate execution with child-outcome collection, merge-conflict check, validation aggregate, and parent-completion blocking; (T145) Phase 3 — bounded adaptive replan node in orchestrationSupervisor with evidence-class triggers and cap enforcement; (T146) Phase 4 — replan decision artifacts documenting trigger evidence, rejected alternatives, task-graph diff; (T147) Phase 5 — human choke points for scope expansion, dependency rewiring, and SCM after contested fan-in. T144 is the first actionable task.
- All acceptance criteria verified: validateFanIn helper, merge-conflict and validation-aggregate checks, FanInRecord persistence, reconciliation integration with parent-revert logic, and 4 unit tests all in place. 972/972 tests pass.
- Implemented bounded adaptive re-planning node: executeReplanNode with trigger classification, cap enforcement, wave truncation, config wiring, and 13 unit tests. 985/985 tests pass.
- All Phase 4 acceptance criteria verified: replanDecisionPath, writeReplanDecisionArtifact, retention guard, orchestrationSupervisor artifact write on replan_applied, RalphStatusSnapshot.replanArtifacts, buildStatusReport Re-planning section, statusSnapshot collection, and unit tests all in place. 994/994 tests pass.
- Implemented three human choke points (scope_expansion, dependency_rewiring, contested_fan_in_scm) in orchestrationSupervisor.ts, each gated behind pipelineHumanGates config. Gate artifacts written to .ralph/artifacts/<parentTaskId>/human-gate-<type>.json. approveHumanReview command extended to clear gate artifacts. 17 unit tests added covering all acceptance criteria scenarios.
- Backlog replenishment required after T147 (Item 23 Phase 5) completed the last task in the Continued multi-agent evolution horizon. All of Items 20-23 are now done: orchestration graph + supervisor ledger (T132-T134), explicit handoff contract lifecycle (T135-T138), role topology + context envelopes + policy enforcement (T139-T142), adaptive re-planning + fan-out/fan-in + human choke points (T143-T147). Added T148-T151 as the next coherent slice: (T148) advance prd.md to record Items 20-23 complete and define next horizon; (T149) v0.3.0 release preparation (CHANGELOG, version bump, dry-run); (T150) orchestration integration smoke test exercising graph + handoffs + role policies + fan-in end-to-end; (T151) dashboard orchestration panel surfacing graph state, node spans, re-plan history, and human gate actions. T148 is the first actionable task; T149 depends on T148; T150 and T151 are independently actionable.
- prd.md marked Continued multi-agent evolution horizon (Items 20-23) complete with 2026-04-16 date and summary; appended Next delivery horizon section with v0.3.0 release, orchestration smoke test, and dashboard panel targets
- v0.3.0 release prep complete: version bumped, CHANGELOG entry added covering orchestration graph/supervisor, handoff contracts, role isolation, fan-out/fan-in, adaptive re-planning, human choke points; publish:dry-run script updated for current vsce; validate + publish:dry-run both pass
- Added opt-in orchestration E2E smoke (scripts/run-e2e-orchestration-smoke.js) and test harness that exercise graph + fan-in + handoff + role policy + scope_expansion human gate; wired test:e2e-orchestration npm script and documented it in docs/testing.md.
- Dashboard Orchestration panel implemented: added OrchestrationPanelSection to dashboardSnapshot, extended collectStatusSnapshot to collect human gate artifacts/fanInRecord/nodeSpans, added stateWatcher for orchestration files, added Orchestration tab in panelHtml, 8 regression tests added. npm run validate passes (1021/1021 tests).
- Backlog replenishment was required after T151 completed the last tracked item from the current horizon slice. Re-inspected the PRD, command surface, shared task-creation pipeline, and dashboard/sidebar infrastructure, then added T152-T155 as the next coherent slice for the still-unshipped "UI task seeding from epic/feature request" capability: (T152) structured epic-seeding engine plus durable seeding artifact, (T153) command-palette seeding flow through shared persistence, (T154) first-class dashboard/sidebar intake path, and (T155) docs plus regression drift guards. T152 is the next actionable task.
- Added a dedicated task seeding engine with strict JSON parsing, durable task-seeding artifacts, shared provider execution reuse, and Add Task command integration through the shared task persistence boundary.
- Added a dedicated command-palette seeding command that shares the existing provider-backed seeding flow with ralphCodex.addTask, hardened appendNormalizedTasksToFile to reject duplicate ids under the task-file lock before writing, updated docs/package metadata, and added regression coverage for success, provider failure, malformed response, and persistence-time id collision cases.
- Verified the existing T154 implementation in the working tree: dashboard and sidebar task-seeding UI, typed DashboardHost seed message/result handling through the shared seeding helper, regression coverage, and docs updates are present. Ran npm run validate successfully.
- Documented the epic/feature task-seeding workflow across README, workflows, invariants, and testing docs; added docs-validator enforcement plus regression coverage for the seeding command/artifact contract; verified with npm run validate.
