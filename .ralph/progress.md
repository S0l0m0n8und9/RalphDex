# Progress

- Ralph workspace initialized.
- Refined `.ralph/prd.md` into a concrete objective for a durable, file-backed multi-agent delivery framework spanning requirements, implementation, testing, and orchestration.
- Replaced the seed backlog in `.ralph/tasks.json` with repo-specific work derived from the actual `ralph-codex-vscode-starter` extension surface and current coverage gaps.
- Completed T3 by validating nested project-root selection for `ralph-codex-vscode-starter`, adding scanner/status regressions, and rebuilding the extension runtime artifacts.
- Completed T4 by adding regression coverage for nested workspace root-selection candidates and prompt repo-context rendering so nested child repos remain visible in generated prompts.
- Completed T5 by adding command-shell smoke coverage for prompt clipboard auto-copy and Open Codex IDE handoff modes, and by documenting the exact clipboard, IDE-command, fallback, and live-host testing boundaries.
- Completed T6 by documenting the manual `.vsix` install workflow, making `npm run package` succeed with `vsce package --no-dependencies` plus repository-aware README links, and validating a fresh `ralph-codex-workbench-0.1.0.vsix` build while capturing the remaining packaging warnings.
- Completed T7 by adding an optional real `codex exec` temp-workspace smoke command, documenting its environment-sensitive usage, and surfacing summarized execution failure messages into the structured Ralph iteration/latest-result/status artifacts after a sandboxed real-run preserved backend-connect failure evidence.
- Advanced T8 by persisting explicit workspace/inspection/execution/verification root policy in prompt evidence, execution plans, CLI invocation artifacts, iteration results, provenance bundles, and status summaries; `codex exec` plus validation/git verifiers now run from the selected inspection root while `.ralph` remains anchored at the workspace root, with nested-root regressions and focused docs updated. Manual override for ambiguous umbrella workspaces is still pending under T8.4.
- Completed T8 by adding the `ralphCodex.inspectionRootOverride` escape hatch for ambiguous umbrella workspaces, validating override paths against the workspace boundary, surfacing applied or invalid override state in scan evidence and status output, expanding override-focused regression coverage, and updating the nested-root docs to reflect the now-complete root policy.
- Completed T9 by locking the shipped automation scope to a sequential single-agent loop, documenting that broader multi-agent orchestration remains deferred until nested root semantics are deterministic and evidence-backed, and adding docs-validation coverage so the guardrail is test-backed.
- Use this file for durable progress notes between fresh Codex runs.
- Fixed the verifier normalization bug that left legacy `cd ralph-codex-vscode-starter && npm run validate` hints unnormalized when the repo itself was the opened workspace. Full `npm run validate` now passes again, but the retention task itself is not yet complete.
- Surfaced generated artifact retention in Show Status, added explicit enabled/disabled coverage, and aligned testing docs with the existing prompt/run/iteration cleanup behavior.
- Made generated-artifact cleanup honor an explicit allowlist of protected state/latest references and added regression coverage so unrelated path-like fields do not preserve old prompts, run artifacts, or iteration directories.
- Made generated-artifact retention precedence explicit as newest-N plus protected references, added regression coverage proving older protected entries remain retained in newest-first order, and aligned the retention docs with that rule.
- Added iteration-engine regression coverage proving blocked-preflight cleanup preserves the prior state-referenced prompt, transcript/last-message pair, and iteration artifact directory when generated-artifact retention is 1.
- Protected generated-artifact cleanup against deleting iteration files that are still referenced by latest-provenance-bundle.json or latest-provenance-failure.json, and added regression coverage for that retention case.
- Aligned the durable retention docs with the implemented behavior: generated-artifact cleanup now explicitly documents protection for paths still referenced by latest provenance bundle/failure pointers, and the testing doc now records that regression coverage.
- Protected generated prompts, run transcripts/last-message files, and iteration directories that are referenced only through durable state runHistory and iterationHistory, with regression and integration coverage.
- Aligned the retention invariants, workflow docs, and testing docs with the implemented generated-artifact protection behavior so `.ralph/state.json` history references (`runHistory[]` and `iterationHistory[]`) are explicitly treated as protected roots alongside `lastRun` and `lastIteration`.
- Extended generated-artifact protection so latest-prompt-evidence.json now protects the implied prompt file and iteration directory, added a regression proving it does not protect unrelated run artifacts, and updated retention docs to enumerate that protected-root rule.
- Added integration coverage proving generated-artifact cleanup preserves an older blocked integrity iteration when latest-provenance-failure.json is the remaining protected reference and retention is 1.
- Extended generated-artifact cleanup so run-only workspace state still protects the corresponding iteration directory, prompt, and transcript/last-message pair, then added regression coverage and doc updates for that fallback behavior.
- Protected prompt/transcript/last-message artifacts from raw state run references even when iteration is missing, added regression coverage for that boundary, and aligned invariants/testing docs.
- Added regression coverage and invariant/testing docs proving generated-artifact cleanup preserves older prompt/run/iteration artifacts when they are referenced only by latest-execution-plan.json or latest-cli-invocation.json. Validation passed with npm test.
- Extended generated-artifact retention so latest-result.json now protects referenced prompt and transcript/last-message artifacts, added regression and integration coverage, and aligned retention docs.
- Closed a remaining generated-artifact retention gap by deriving protected iteration references from lastRun when iterationHistory is absent, and added regression coverage for the lastRun-only state shape.
- Added explicit regression and doc coverage proving latest-preflight-report.json protects only the referenced iteration directory during generated-artifact cleanup; npm test passed.
- Made generated-artifact retention precedence explicit in the high-level README and architecture docs: Ralph keeps the newest parsed-iteration window first, then adds older protected references without evicting newer retained entries.
